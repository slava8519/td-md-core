# УТОЧНЁННЫЙ ROADMAP РАЗРАБОТКИ
## TD-MD Core · v1.0 · 2026-06-05 · расширяет ТЗ §9 (Спринты)

> **Статус (2026-06-11):** ✅ M0 · ✅ M1 · ✅ M2 · ✅ M2.5 · ✅ M2.6 — критерии приёмки
> зелёные (6 тестов в CI). **Текущая веха: M2.7 (GPU bring-up).** Сверка формул диссертации с кодом —
> `docs/_meta/FORMULA_VERIFICATION_2026-06-11.md` (семантика C3/ур.62 исправлена по ней).
> План пересмотрен по второму архитектурному ревью `docs/_meta/ARCH_ROADMAP_REVIEW_2026-06-11.md`
> (находки B1–B10): добавлены вехи **M2.6** (тепловая инициализация) и **M3.5**
> (TimeConveyor-CPU); уточнены M3 (Tier-split, список пар), M4 (порт на CUDA,
> фикс-точка), M5 (k шагов/узел), M6 (партиальная ρ для EAM).
> **Ревизия 2026-06-11 (GPU-аудит):** разработка переехала на машину с целевой GPU
> (RTX 5080, sm_120, CUDA 13.1) — план скорректирован по
> `docs/_meta/ARCH_AUDIT_GPU_2026-06-11.md` (38 подтверждённых находок): добавлена
> веха **M2.7** (GPU bring-up), M5 разбита на **M5a/M5b**, Tier-2-проверки
> размещены в M2.7/M3, перф-критерии привязаны к режиму точности
> (измеренный разрыв FP64:FP32 = 1:66.7), `production_mixed` перенесён M6→M4,
> NVE-критерий M3.5 переопределён (старый порог 1e-5/50k эмпирически непроходим).

**Зачем.** Спринты ТЗ §9 слишком крупные для пошаговой ИИ-разработки (например «Спринт 3: конвейер + автомат + события + multi-GPU» — это месяцы без промежуточных ворот). Здесь — мелкие вехи `M0…M7`, каждая с **критерием приёмки**, привязанным к VerifyLab. Правило: переход к следующей вехе только после зелёного критерия текущей.

**Ключевой принцип — Walking Skeleton (A5).** Сначала сквозной минимальный прогон без TD-сложности, затем наращивание. Это рано вскрывает ошибки единиц/сил/интегратора, пока их дёшево чинить.

---

## ✅ M0 — Walking Skeleton (сквозной минимум) — DONE 2026-06-06
- **Цель:** запускаемый бинарь без TD-конвейера: 1 зона, 1 стрим, Морзе, NVE, FP64, фиксированный $\Delta t$.
- **Делается:** CMake (C++20), парсер `config.yaml` (мин. набор), чтение `al_fcc_72.data`, наивный $O(N^2)$ расчёт Морзе, скоростной Варлет, запись `.lammpstrj`.
- **Критерий приёмки:** `Test_0_Step` — силы совпали с `reference_data/reference_forces.csv` ($\le10^{-6}$ эВ/Å); 100 шагов NVE без NaN; энергия дрейфует слабо.
- **Закрывает риски:** единицы (A1), базовая корректность сил/интегратора.
- **Соответствие ТЗ:** часть Спринта 1 + 2.

## ✅ M1 — Конечный автомат зон (изолированно) — DONE 2026-06-06
- **Цель:** `ZoneFSM` как чистая логика, без GPU.
- **Делается:** enum состояний/событий, таблица переходов и guard'ы (`ZoneFSM §4`), флаг частичности (INV-3).
- **Критерий:** юнит-тесты всех допустимых переходов и отклонения недопустимых; цикл `o→d→w→c→p→o` воспроизводится; стартовый seed-случай $S_1$ и чётный/нечётный порядок (`ZoneFSM §7`).
- **Соответствие ТЗ:** ядро Спринта 3 (вынесено вперёд и изолировано).

## ✅ M2 — Буфер причинности и авто-шаг — DONE 2026-06-06 (семантика C3 уточнена 2026-06-11 по ур.62)
- **Цель:** защита причинности и динамический $\Delta t$.
- **Делается:** `R_buf=v_{max}\Delta t C` (ур. 33), `causality_ok` (INV-4), авто-шаг по $C_1/K_2/C_3$ (Гл. 3.3, 3.5), $v_{max}$ через Parallel Reduction; локальный $v_{max}$ без глобального барьера (A8).
- **Критерий:** тест буфера; искусственное $v_{max}\Delta t>R_{buf}$ → HALT + `rescue.xyz`; авто-шаг стабилен при $C_3=0.5$.
- **Соответствие ТЗ:** Спринт 2 (динамический шаг/буфер).

## ✅ M2.5 — Ранний occupancy-зонд (early go/no-go)  [ENG] — DONE 2026-06-06
- **Зачем (риск A2).** TD хочет тонкие зоны (~$R_{cut}$), GPU — десятки тысяч нитей. Одна тонкая зона может недогрузить чип так, что TD на одном GPU окажется *медленнее* наивной MD. Это make-or-break, и проверять надо **до** дизайна кластеризации M3, а не после: зонд даёт ранний go/no-go и числа (атомов/зону, нитей/SM, во сколько зон батчить), на которых строится M3.
- **Контекст железа.** *(На момент M2.5)* локально — Mac mini M4 Pro, NVIDIA/CUDA нет. **Устарело 2026-06-11:** разработка переехала на Linux + RTX 5080 (sm_120); Tier-2 разморожен и выполнен в M2.7, измеренный профиль — `_meta/ARCH_AUDIT_GPU_2026-06-11.md` §0 (модель Tier-1 подтверждена с одной поправкой: 24 блока/SM, не 32). Исторический контекст зонда — зонд был спроектирован **двухуровневым**:
  - **Tier 1 — аналитический, локально без GPU.** Чистый C++/Python: по (`zone_width`, плотность, конфиг запуска ядра, оценка регистров/shared mem на нить) считает теоретическую occupancy и атомов/варпов на зону для эталонного Al и набора размеров; моделирует одиночную зону и батч из $k$ зон на стрим. Спецификации целевого GPU — из профиля устройства (или `cudaGetDeviceProperties` на железе), **догадки не хардкодить**.
  - **Tier 2 — эмпирический, на NVIDIA GPU/кластере (отложен).** `cudaOccupancyMaxActiveBlocksPerMultiprocessor` (теор.) + Nsight Compute `sm__warps_active…pct_of_peak` (achieved) на реальном ядре сил. За CUDA-гейтом; **CI не блокирует**.
- **Делается:** утилита `occupancy_probe` (Tier 1) + committed-отчёт `docs/_meta/occupancy_probe_<date>.md`: кривая occupancy vs ширина зоны / атомов на зону + рекомендация (atoms-per-block, число одновременных зон на стрим для насыщения цели).
- **Критерий приёмки:** Tier 1 собирается и прогоняется на M4 Pro; для эталонного Al и целевого GPU-профиля зафиксированы атомов/зону при `zone_width=R_cut`, предсказанная occupancy одиночной зоны и батча. Если одиночная зона ниже целевого порога загрузки — зонд **обязан** показать, что батчинг её восстанавливает; иначе явный red-flag и пересмотр стратегии зон до входа в M3. Tier 2 — отложенный пункт чек-листа (при доступе к GPU), не гейт CI.
- **Соответствие ТЗ:** Спринт 2 (нефункциональный критерий occupancy, A2). **Питает дизайн M3.**

## ✅ M2.6 — Тепловая инициализация (Maxwell, seed)  [B8] — DONE 2026-06-11
- **Цель:** реалистичные прогоны при $T>0$; задействовать `run.seed` (сейчас все тесты стартуют с $T\approx0$, где K2 и буфер почти не нагружены; диссертация тестирует тепловые режимы §3.5–3.6).
- **Делается:** максвелловское распределение скоростей при `run.init_temperature` (ГПСЧ от `seed`), обнуление импульса ЦМ; чтение секции `Velocities` в `reader_lammps`; `Test_NVE_Drift` при T=300 K + **проверка сохранения импульса**.
- **Критерий приёмки:** $T(0)$ = заданной (±5%); $|p_{ЦМ}|\le10^{-12}$; NVE-дрейф при 300 K зафиксирован численно; два прогона с одним `seed` побитово совпадают.
- **Закрывает:** B8; предусловие честной валидации M3.5/M4.
- **Итог (2026-06-11).** `tdmd/core/thermal.hpp`: собственный ГПСЧ SplitMix64 + Бокс—Мюллер (std::normal_distribution непереносим между stdlib — нарушал бы INV-9), фиксированный порядок выборки; обнуление импульса ЦМ (2 прохода — второй давит остаток округления с ~1e-12 до ~1e-13 на «неудачных» seed) и точный рескейл к T на $n_{dof}=3N-3$ (семантика LAMMPS `velocity create`). `reader_lammps` читает `Velocities` (сопоставление по id); `run.init_temperature` в конфиге (>0 — Maxwell от seed, перекрывает Velocities; 0 — скорости из файла). Измерено (golden Al-72): T(0)=300 K точно; $|p|\le2\cdot10^{-13}$ amu·Å/ps по всем компонентам (5 seed); NVE-дрейф 300 K, dt=1 фс, 100 шагов: rel=7.1e-4 (тест-порог 3e-3); импульс после 100 шагов ≤6e-13; два прогона движка с одним seed — `cmp` побитово идентичны. `config_auto.yaml` переведён на тепловой старт 300 K: авто-шаг под нагрузкой опускает dt 5 фс → 0.53 фс (K2/C1 реально работают). Тесты: `Test_Thermal` (7 кейсов) + `Test_NVE_Drift.EnergyAndMomentumConservedAt300K`.

## ✅ M2.7 — GPU bring-up (Tier-2 зонда, CUDA-каркас сборки)  [ENG] — DONE 2026-06-11
- **Зачем.** Разработка переехала на машину с целевой GPU (RTX 5080, sm_120, CUDA 13.1) — отложенные «до доступа к GPU» Tier-2-пункты M2.5 разморожены, а CMake до сих пор не имеет ни одного CUDA-крючка. Микро-веха (~день) снимает это до входа в M3, чтобы дизайн кластеризации опирался на измеренные, а не провизорные числа.
- **Делается:** (а) `tools/devprobe.cu` — deviceQuery + микробенч (FP32/FP64 FMA, контендед atomicAdd f64 vs u64, D2D bandwidth) → committed-отчёт `docs/_meta/gpu_baseline_<date>.md`; (б) исправление Tier-1 зонда по измерениям (24 блока/SM вместо 32, CLI-переопределения), перегон отчёта M2.5 со снятием PROVISIONAL; (в) `option(TDMD_WITH_CUDA)` + `CMAKE_CUDA_ARCHITECTURES=120-real` + smoke-ядро (Морзе-функция на device, сверка с CPU ≤1e-12) + тест с меткой `cuda` (не гейт облачного CI).
- **Критерий приёмки:** smoke-тест зелёный локально; отчёт gpu_baseline зафиксирован; отчёт зонда перегнан с фактическими параметрами устройства; CPU-сборка без `TDMD_WITH_CUDA` не затронута (6 тестов зелёные).
- **Закрывает:** Tier-2 M2.5 (deviceQuery-часть; Nsight-часть требует ядра — уходит в M3), каркас для прототипного ядра M3.
- **Итог (2026-06-11).** `tools/devprobe.cu` + `docs/_meta/gpu_baseline_2026-06-11.md`: подтверждено 84 SM / 48 warps/SM / **24 blocks/SM** / 65536 regs / 100 KiB smem; FP32 53.5 TFLOP/s, FP64 0.81 (1:66), atomicAdd(double) ×40 медленнее u64, D2D 822 GB/s; `cudaOccupancyMaxActiveBlocksPerMultiprocessor` на референс-ядре (блок 128) = 12 блоков/SM, 100% — **Tier-1 модель подтверждена точно**. Зонд: дефолт 24 blocks/SM, флаги `--blocks-per-sm/--smem-block/--date`, условный баннер confirmed; отчёт перегнан → `occupancy_probe_2026-06-11.md` (PROVISIONAL снят, вердикт устоял: батч ≈9 зон при N=10⁶); тесты профиля 5080 и потолка «кластер=блок» (50%) в `Test_Occupancy`. CMake: `TDMD_WITH_CUDA` (OFF по умолчанию), `CMAKE_CUDA_ARCHITECTURES=120-real`, `Test_CUDA_Smoke` (метка `cuda`; FP64-сверка device↔host 2.7e-15); `scripts/gpu_gate.sh` — ctest -L cuda + compute-sanitizer memcheck/racecheck (0 ошибок).

## ✅ M3 — Warp-Aligned Clustering + пространственная сортировка  [ENG] — DONE 2026-06-12
- **Цель:** убрать $O(N^2)$, обеспечить коалесценцию. *(Тег [ENG]: замещает диссертационную сетку «ячейка ≤1 атома» Гл. 3.1 — см. ТЗ §3.2.)*
- ✅ **Сделано (первый PR — гигиена B10, 2026-06-12):** строгая валидация конфига (enum'ы — fatal, включая `timestep.mode`/`boundary`; диапазоны C1∈(0,1], C_buf≥1, dt>0 и пр.; неизвестные ключи — warning с именем; все ошибки собираются за один проход; `tdmd_config`-библиотека), ридер: `mass>0` для всех типов (было: NaN без диагностики), try/catch (битый файл → сообщение, не terminate), дубликаты id, метки `xu yu zu` в писателе, HALT при перекрытии (r<0.5 Å, exit 4, restart-capable rescue; `last_min_r2` трекается в compute() бесплатно), `Test_Config` (16 кейсов) + `Test_Reader` (9).
- ✅ **Сделано (данные, 2026-06-12):** `tools/gen_fcc.py` — параметрический генератор ГЦК (побитово воспроизводит golden-геометрию при `--cells 3 3 2`); закоммичен `al_fcc_144.data` (3×3×4, ≥4 зоны по z); 10⁴/10⁵ — on-demand (эталонные силы не хранятся — оракул это кросс-тест «кластер ≡ O(N²)»).
- **Делается (ядро вехи):** ✅ *(2026-06-12, CPU-часть)* сетка-сортировка Z-order (`core/cluster.hpp`: Morton 21 бит/ось, детерминированный ключ (morton, id), in-place permute SoA — идентичность атомов в новом поле `id`) → упаковка в 32-атомные кластеры (роль сетки — A9); **список пар кластеров со skin-слоем** (full-neighbour — решение в `Rationale`; AABB-куллинг с min-image; `neighbor.skin`, перестройка при накопленном смещении > skin/2; `neighbor.mode: direct|cluster` в конфиге, движок интегрирован — `ClusteredMorse` тот же контракт compute, что у эталона) с перестройкой по накопленному max-смещению (авто-шаг C1 ограничивает смещение ≤ C1·cell/шаг ⇒ кадрность перестройки предсказуема); NVTX-маркеры (отложены к M4); `cub::DeviceRadixSort` на GPU-пути — carve-out ТЗ §2.2 (M4). ✅ *(2026-06-12)* Разрез `MorsePotential`: парный функтор `pair_morse.hpp` (host/device через `hal.hpp`/`TDMD_HOST_DEVICE` — единый источник парной математики для O(N²)/кластерного/CUDA путей) + reference O(N²)-драйвер; обнуление сил — у вызывающего (`core::zero_forces`, compute() накапливает — нужно w-механизму M3.5); нумерика пути сохранена (прогон config_auto побитово совпал с дорефакторным). ✅ *(2026-06-12)* `run_simulation()` извлечён в `core/simulation.hpp` (I/O-free, фрейм-коллбек, `SimResult`/`Halt`) + **`Test_Determinism`**: run-to-run побитово (memcmp x/v) в fixed и auto режимах — страховочная сетка до перетряски порядка пар; INV-4 проверяется всегда (тест: fixed-режим + лёгкие атомы на отталкивающей стенке → HALT Causality). ✅ *(2026-06-12)* Кластерный драйвер `ClusteredMorse<Real>` поверх того же функтора: **кросс-тест «кластер ≡ O(N²)»: max|ΔF| = 8.9e-16 (golden 72), 1.3e-15 (N=10 976) — критерий ≤1e-12 с запасом 3 порядка**; PE ≤4e-11 абс. на 11k; побитовый run-to-run на кластерном пути (несмотря на in-place сортировку — ключ детерминирован); **Real=float инстанцирован**: парная математика в FP32 при FP64-координатах/разностях/аккумуляции (контракт production_mixed §3.3 mixed-precision дока; cutoff решается в FP64 ⇒ множество пар идентично эталону), max|ΔF| fp32 = 1.4e-6 эВ/Å (допуск 1e-4). **Тайминг (bench_pair, 1 ядро CPU): ×9.8 атомов → прямой ×89.9 (N²), кластерный ×9.4 (линейный); на N=10⁵ кластерный быстрее ×38** — уход от O(N²) подтверждён. Остававшееся закрыто 2026-06-12 — см. бюллет «Сделано (закрытие вехи)» ниже. Маппинг **≥2 кластера на блок** (измеренный лимит 24 блока/SM: 1 кластер=1 блок из 32 потоков ⇒ потолок 50% occupancy; рекомендация — блок 128).
- ✅ **Сделано (Tier-2, 2026-06-12):** прототипное CUDA-ядро по списку пар кластеров (`tests/test_cuda_cluster.cu`: 1 warp = 1 кластер, блок 128 = 4 кластера, shared-staging соседей, owner-writes без атомиков; тот же функтор и формат списка, что у CPU-драйвера). **Фактические регистры: 56 (fp64-путь) / 62 (fp32 — конверсии добавляют временные); 9/8 блоков/SM = 75%/67% теоретической occupancy** — ниже 100%-линии (42 рег), выше красного флага 50% (A2); Tier-1 модель воспроизводит runtime-API точно (дописано в `occupancy_probe_2026-06-11.md`). GPU ≡ CPU-кластерному ≤5.1e-15 эВ/Å (N=10 976); sanitizers чистые. Achieved occupancy (`ncu`) измерена 2026-06-12 (профилирование разблокировано: `RmProfilingAdminOnly=0`): **6.5% (fp64, N=10 976) при 75% теоретических** — дефицит варпов в полёте (grid 86 блоков ≈ 1/SM при ёмкости 756), для насыщения нужно ≈10⁵ атомов/запуск; прогноз M2.5 подтверждён измерением — `_meta/ncu_occupancy_2026-06-12.md`. Давление регистров — рычаг оптимизации M4.
- ✅ **Сделано (закрытие вехи, 2026-06-12):** **(1) LJ**: функтор `pair_lj.hpp` (host/device, + аналитический LRC-хелпер) и общий драйверный слой `pair_driver.hpp` (циклы O(N²)/кластерный извлечены, обёртки Morse/LJ × direct/cluster без дублирования; рефакторинг побитово прозрачен — траектории config_auto/cluster идентичны дорефакторным); `potential.type: lj` + `potential.lj{epsilon,sigma}` в конфиге. **(2) Схемы обрезки** `potential.truncation: cut|shift|force_shift` (`cutoff.hpp`, политика драйвера в FP64; решение force-shift vs switching — `Rationale`; дефолт `shift` сохранён). **(3) Внешний эталон NIST SRSW**: `Test_LJ_NIST` — энергия+вириал на 4 замороженных конфигурациях × 3 схемы NIST (cut+LRC rc=3σ/4σ, LFS rc=3σ), все 32 проверяемых значения в допуске печати NIST (rel ≤1e-4; 4 тождественных нуля U_LRC LFS-схемы относительным допуском непроверяемы); вириал `last_virial` = −Σ r·dU/dr добавлен обоим драйверам; кластерный путь ≡ прямому на LJ ≤5.7e-14; fp32-инстанциация LJ 1.1e-5. Данные NIST вербатим + полная таблица: `reference_data/nist_lj/README.md`. **(4) Кадрность перестройки vs C1**: формальные тесты — баллистический газ (интервал ТОЧНО равен ceil((skin/2)/(C1·cell))=3) и термический 300 K (нижняя граница из измеренного max-смещения; решётка при skin=1.0 не перестраивается вовсе — это норма Верле-таблицы). **(5) Grid-куллинг пар кластеров**: loose grid (растеризация AABB по ячейкам, CSR + stamp-дедуп) + точный AABB-тест ⇒ список бит-в-бит равен O(C²)-эталону (кросс-тест PBC/free/mixed/узкий бокс); Z-страддлеры и два отвергнутых варианта (центр-биннинг ×1, разделение выбросов ×3 — страддлеров 12%) — `Rationale`; замер 10⁶: построение списка 229 мс против 3.18 с O(C²) (×14, включая Z-сортировку), масштабирование 10⁵→10⁶ линейное. **(6) Фикс INV-4**: холодный старт (v=0, fixed dt) детерминированно падал HALT на шаге 2 (разгон удваивает v_max за шаг > C_buf) — буфер теперь по прогнозу v_pred = v_max + a_max·Δt ([ENG]-уточнение ур.33, `Rationale`); честные нарушения (пролёт в стенку сквозь кат-офф) ловятся — тест переработан + регрессионный тест холодного старта.
- **Критерий (Tier-1, локально/CI):** силы кластерного пути ≡ силам прямого $O(N^2)$-пути (≤1e-12, внутренний кросс-тест, N до 10⁴ в CI; 10⁵ — nightly/вручную) и golden (≤1e-6); таймингом подтверждён уход от $O(N^2)$ (N=10⁴ vs 10⁵, google/benchmark); частота перестройки списка соответствует C1-оценке; run-to-run bitwise зелёный; fp32-инстанциация собирается и проходит допусковый тест; **внешний эталон корректности LJ** — энергия/вириал на замороженных конфигурациях NIST SRSW (точное совпадение схемы обрезки), см. валидационную лестницу `_meta/VALIDATION_EXPERIMENT_2026-06-12.md` §2,§6 (LJ — флагманская визитка проекта, реплика §3.5 на одной GPU при N=10⁶–10⁷).
- **Критерий (Tier-2, локально, не гейт облачного CI):** профиль ncu прототипного ядра без явного Thread Divergence; **achieved occupancy соответствует прогнозу M2.5/M2.7** — не ниже целевого порога, либо зафиксировано расхождение модель/факт с объяснением (A2); фактические регистры/поток внесены в отчёт зонда.
- **Соответствие ТЗ:** Спринт 2.

## ✅ M3.5 — TimeConveyor-CPU (логика TD без CUDA)  [ENG] [B6] — DONE 2026-06-12
- **Цель (переквалифицирована 2026-06-11):** изолированная отладка конвейерной логики (B1–B3) + **постоянный конвейерный тест облачного CI** (ubuntu-latest без GPU) + CPU-референс для сверки с GPU-путём M4 (референс допусковый, не побитовый: libm и CUDA-math расходятся в последнем ulp). Исходный мотив B6 «критерий M4 нельзя прогнать локально» снят переездом на GPU-машину — веха сохраняется ради CI и отладки, **таймбокс**: CPU-производительность кольца не полировать.
- **Делается:** z программных узлов (`std::jthread` + SPSC-очереди как заглушка `ITransport`), ≥4 зон, оркестровка через `ZoneFSM`, чёт/нечёт I/O; **Δt-handoff** (B2/ZoneFSM §6: Δt следующего шага решает узел-отправитель по v_max последнего завершённого прохода и передаёт с первой зоной; правило тождественно при z=1); **PBC-замыкание** (B3/ZoneFSM §7.2: циклические индексы зон, партнёр-`d` со сдвигом шага, удержание первых зон); **фикс-точечное накопление сил** (B1). Допускается параллельность с подготовкой M4 (StreamTransport, events, фикс-точка на GPU); жёсткий гейт один — критерий M4 прогоняется только после зелёного M3.5.
- ✅ **Сделано (первый PR, 2026-06-12):** **B1 фикс-точка** — `core/fixed_accum.hpp` (int64 Q24.40 силы / Q34.30 энергия, ≡ AMBER SPFP; округление rint/FE_TONEAREST = CUDA `cvt.rni` — задел на побитовую сверку CPU↔GPU в M4) + **зонная декомпозиция и w-механизм** — `core/zones.hpp` (нарезка по z, ширина ≥ r_cut — fatal иначе; запрет 2 зон при PBC z — замыкание совпало бы с прямым интерфейсом; проход по зонам: внутренние пары + кросс-пары S_i↔S_{i+1} однократно с Ньютоном-3, PBC-замыкание S_n↔S_1 — §7.2/B3; накопление фикс-точечное ⇒ сборка побитово не зависит от порядка обхода зон). `Test_Zones` (7 тестов): **сумма w-вкладов ≡ монолит ПОБИТОВО** (сильнее старой формулировки ≤1e-12; vs FP64-оракул — 4.0e-12, в границе кванта Q24.40), **INV-8 однократность каждой пары** через инструментированный хук (защита от полного пересчёта), порядконезависимость побитово, free-граница без замыкания. Дальше: кольцо `std::jthread`+SPSC, Δt-handoff, реплика §3.6.
- ✅ **Сделано (второй PR, 2026-06-12): кольцо TimeConveyor-CPU.** `core/transport.hpp` (ZoneMsg/ограниченные SPSC-каналы с halt-aware shutdown; `RingTransport` — CPU-заглушка `ITransport` Skeleton §2.1) + `core/conveyor.hpp`: узел = `std::jthread`, проход = модельный шаг (узлу $P_k$ — шаги $k, k{+}z, \dots$), зона уходит следующему узлу **сразу после END** (поконвейерно, как в первоисточнике стр. ~1549); оркестровка строго через `ZoneFSM` (T1–T5, seed §7.1, INV-1 assert), чёт/нечёт I/O §7.4; **Δt-handoff (B2)** позиционной Λ-цепочкой — лаг светового конуса $n{-}1$ (ZoneFSM §6 п. 3–5) ⇒ последовательность Δt **побитово не зависит от z**; **PBC-замыкание ротацией** (§7.2 [ENG]: голова удерживается в `c` до хвоста, пары и позиции ≡ первоисточнику); INV-4 на END каждой зоны ($v_{pred}$ с лагом $L{=}\max(1,n{-}1)$); стражи Overlap/NonFinite/**StaleZone** (статическое членство, миграция → M4); общее парное FP-ядро с последовательным проходом (`PairGeom` в zones.hpp) и общие поатомные ядра редукций (buffer.hpp) — рефакторинги побитово прозрачны (md5 траектории config_auto не изменился). **Тесты:** `Test_Conveyor` (8): конвейер ≡ последовательному степперу на `zone_force_pass` побитово (fixed; и 1 зона ≡ 4 зонам — следствие B1), **1 узел vs z∈{2..5} побитово (fixed и auto с C3=1; dt-последовательности равны поэлементно)**, PBC-замыкание ≡ оракулу + сохранение импульса, anti-deadlock z=1..6 по ≥2z шагов, HALT INV-4, HALT StaleZone, **реплика §3.6: Al-72, свободные границы, авто-шаг (C1=10 дисс.), 25 900 шагов, 1 vs 4 узла — нулевое отклонение координат и скоростей (рис. 43–44)**; `Test_NVE_Invariant`: 50 000 шагов dt=1 фс T=300 K — многозонный конвейер **побитово ≡** 1-зонному эталону (сильнейшая форма «не хуже»); секулярный тренд (LSQ-фит) **−1.20·10⁻² kT/(нс·dof)** [deterministic_fp64, morse+shift] — пол задан разрывом силы схемы `shift` на $R_{cut}$ (аудит C1, force_shift снизил бы пол), потолок калиброван 3× измеренного; импульс ≤1e-8. Интеграция конвейера в CLI/config — отложена к M4 (ключи `decomposition.ring` в ConfigSchema готовы; движок пока ходит через `run_simulation`). **Adversarial-ревью кольца** (29 агентов, 4 измерения + верификаторы с репро/TSan/Debug-сборкой; конкурентность чиста — TSan и матрица дедлоков 564 конфигураций): исправлено — мёртвый-в-Release/ложный-в-Debug assert INV-1 (заменён на проверку `computed`-флага + исключение→`Internal`; Debug-сборка теперь зелёная), молчаливое UB-переполнение int64 Q24.40 на крутых потенциалах (LJ пересекает ±2²³ эВ/Å уже при r≈0.86 Å — `FixedAccum::add` теперь бросает исключение; NaN/inf тоже ловятся), лаг Δt-правила при n=1 (решение перенесено в момент первой отправки ⇒ n=1 ≡ последовательному правилу, тест-оракул `SerialAutoRuleOracleN1` + replay-оракул Λ-рекуррентности по записанным агрегатам в `PassStats` — мутации цепочки больше не проходят сьют), совпадающие пары видны Overlap-стражу (min_r2 до отсева, как в драйверах), учёт `steps_done`/stats на HALT (только достоверно завершённые проходы), зависание `run()` при частичном спавне потоков, маска вкладов T2 реализована + проверяется на END (INV-3), освобождение аккумуляторов при SEND (INV-7-гигиена), покрытие PBC+auto и вырожденных входов (пустые зоны под ротацией несут dt-заголовок, steps=1, z>steps), `Zone::step_h` расширен до long. Замечание о чётности §7.4: с буферизованными каналами ёмкости n+2 send не блокируется (измерено) — порядок чёт/нечёт станет нагруженным на rendezvous-транспорте M5a; задокументировано в тесте.
- **Критерий приёмки:** `Test_determinism` — побитовое совпадение координат **и скоростей** (методика Гл. 3.6) 1 узел vs z узлов, в т.ч. при `timestep.mode=auto`; **репликация эксперимента §3.6** — Al-72, свободные границы, авто-шаг (C1=10 по диссертации), ≥25 900 шагов, побитово 1 vs z узлов (покрывает путь «кольцо без PBC-замыкания»); **ярлык: историческая реплика детерминизма конвейера, НЕ измерение плавления** (72 атома не имеют резкого перехода, см. `_meta/VALIDATION_EXPERIMENT_2026-06-12.md` §4); **сборка сил из частичных вкладов** — кросс-тест «сумма w-вкладов ≡ монолитный расчёт ≤1e-12» + тест однократности пары через границу зон (INV-8; защита от «читерской» реализации полным пересчётом); `Test_NVE_Invariant` 50 000 шагов: **секулярный тренд** $E_{full}$ (линейный фит, отделён от осцилляции Верле) в индустриальных единицах **kT/(ns·dof)** (реперы OpenMM, см. `_meta/MIXED_PRECISION_BESTPRACTICES_2026-06-11.md` §3.5: `deterministic_fp64` ориентир ~$10^{-7}$, `production_mixed` ~$2\cdot10^{-5}$ kT/(ns·dof)) при фиксированных dt=1 фс, T=300 K, режим точности указан — **многозонный конвейер не хуже 1-зонного эталона той же длины** (порог калибруется эталонным прогоном в начале вехи; абс. значение зависит от разрыва силы на $R_{cut}$ — см. аудит C1, решение force-shift/switching принять в M3); сохранение импульса; anti-deadlock ≥ 2z шагов; работает в CI. *(Старый порог «дрейф ≤1e-5 за 50k» отменён: измерено rel=1.46e-2 при 1 фс и dt-независимый пол из-за разрыва силы на кат-оффе.)*
- **Закрывает:** B1, B2, B3, B6; логическую часть A3. **Питает M4.**

## M4 — TimeConveyor: одно-GPU эмуляция (порт M3.5 на CUDA)
- **Цель:** перенос проверенной логики конвейера на массив `cudaStream_t`; первые честные перф-числа.
- **Делается:** `StreamTransport` (`cudaMemcpyAsync`, pinned-буферы — 2 copy-engines без page-locked памяти не утилизируются), аппаратная синхронизация `cudaEvent_t` (INV-2); **фикс-точечное накопление на GPU** (B1; FP64-`atomicAdd` — только как референс сверки: порядок не фиксирован и на consumer-Blackwell он ~38× медленнее int64); режим `deterministic_fp64` ⇒ FP64-offsets и транспорт (B5); **инстанциация `production_mixed`** (перенесено из M6: на 1:66.7 перф-замеры в FP64 измеряют пропускную способность двух FP64-юнитов/SM, а не дизайн конвейера); **дизайн-вопрос точности координат на handoff** (B5/MINT32: дрейф mixed гонят координаты, не FP32-силы — выбрать int32-фикс-точку offsets/double-single vs FP32 до фиксации формата зоны, см. `MIXED_PRECISION_BESTPRACTICES_2026-06-11.md` §3.4); verify-сборка с `--fmad=false` (детерминизм-сравнения CPU↔GPU); **бенчмарк-baseline «TD-конвейер vs наивный single-kernel путь»** + методика `docs/TD_MD_Core_Bench_v1_0.md` (atom-steps/s, N=10⁵–10⁶, режим `production_mixed`, прогрев/повторы/версии драйвера) — фиксация коэффициента и условий (A2), **не** pass/fail-гейт (ТЗ §1: цель — multi-device масштабирование малых систем).
- ✅ **Сделано (первый PR, 2026-06-12): B1 фикс-точка на GPU + разблокировка ncu.** Профилирование разблокировано (`RmProfilingAdminOnly=0`), achieved occupancy ядра M3 измерена и сопоставлена с прогнозом — `_meta/ncu_occupancy_2026-06-12.md` (6.5% при 75% теоретических: дефицит варпов, прогноз M2.5 подтверждён). Ядро `cuda/zone_force.cuh`: зонный силовой проход (внутренние пары + двусторонние кросс-запуски ≡ структуре CPU-прохода), **квантование на каждый парный вклад** (device `rint` ≡ `std::rint`/FE_TONEAREST), целочисленные регистровые частичные суммы + один `+=` на атом (ассоциативность ⇒ независимость от тайлов/блоков/порядка запусков), энергия — целочисленная блочная редукция + atomicAdd; full-neighbour даёт побитово те же ±q, что half-list Ньютона-3 на CPU (точность отрицания IEEE + симметрия rint); переполнение — sticky-флаг (atomicOr, host обязан трактовать как Halt::Internal; CPU-близнец бросает исключение), min_r2 — atomicMin на битах (B10); `PairGeom::reduce`/`CutoffScheme::apply` — те же host+device функции, что у CPU (один источник). `Test_CUDA_Zones` (verify-сборка `--fmad=false`, метка cuda): **LJ — сырые int64-аккумуляторы сил и энергии CPU ≡ GPU ПОБИТОВО** (сильнее допускового критерия; LJ свободен от трансцендентных), Морзе — допуск 1e-12 (измерено 0.0: квант Q24.40 ≈ 9.1e-13 поглощает ulp-расхождения exp, расхождение возможно лишь на границе округления кванта) + GPU-внутренняя побитовость (run-to-run, обратный порядок запусков), sticky-флаг на LJ при r=0.5 Å, регистры/occupancy: 69 рег (fmad=false), 7 блоков/SM = 58% теоретических (выше красной линии 50% A2); gpu_gate (memcheck+racecheck) чистый ×3 бинаря.
- ✅ **Сделано (второй PR, 2026-06-12): StreamTransport + кольцо на стримах.** `cuda/zone_integrate.cuh` (дрейф/кик Верле, конверсия raw→FP64, зонные редукции v_max²/a_max²/k2cap через atomicMax/Min на упорядоченных битах — точные, порядконезависимые; KE — Q34.30) — **всё побитово ≡ CPU-выражениям** (общие host+device ядра buffer.hpp). `cuda/conveyor_gpu.cuh`: **GpuTimeConveyor** — узел = `cudaStream_t` + хост-`jthread` (протокол M3.5 дословно: ZoneFSM T1–T5, Λ-цепочка, ротационное замыкание, чётность §7.4, стражи); **StreamTransport**: заголовки зон — хост-SPSC, полезная нагрузка — упакованный `cudaMemcpyAsync` D2D в слот потребителя с **двумя кольцами событий `cudaEvent_t`** (arrival: INV-2 аппаратно «END+SEND happens-before RECV»; free: защита переиспользования слота, INV-5/6) и **кредитной очередью свободных слотов** (потребитель возвращает кредит ПОСЛЕ записи free-события ⇒ ожидание никогда не вырождается в no-op CUDA-семантики незаписанного события; модульная нумерация слотов ломалась на PBC-ротации — голова уходит последней, слот затирался: пойман тестом, min distance 0); END-скаляры — pinned-staging + sync, решения INV-4/Δt — на хосте как в CPU. `Test_CUDA_Conveyor` (5, verify `--fmad=false`): **LJ fixed и auto: GPU-конвейер ≡ CPU-конвейеру ПОБИТОВО** (координаты+скорости+pe+dt+v_max-последовательности) и **1 стрим vs N стримов побитово** (ячейка deterministic_fp64 матрицы INV-9), PBC-замыкание ≡ CPU, **реплика §3.6 на GPU: Al-72, авто-шаг, 25 900 шагов, 1 vs 4 узла — нулевое отклонение**; INV-4 HALT через device-редукции. Кросс-платформа на Морзе — допусковая по контракту: за 25 900 шагов единичные квант-флипы exp (вероятность ~2e-5/вклад) дают ляпуновское расхождение траекторий (измерено 70 Å на свободном кластере) при строгой побитовости внутри платформы — это и есть систематика матрицы INV-9. Санитайзеры: memcheck/racecheck чистые (реплика исключена из racecheck-прогона по времени — тот же код-путь, что короткие тесты).
- ✅ **Сделано (третий PR, 2026-06-12): production_mixed + решение B5.** **Решение B5/MINT32** (полный разбор — `Rationale`): транспорт зон в `production_mixed` — **int32-фикс-офсеты со степенью-двойки квантом** (позиции: минимальная 2⁻ᵐ покрывающая бокс+64 Å в int32; скорости 2⁻²⁴, силы 2⁻²⁰; выход за диапазон — sticky-флаг → HALT `Internal`), а не FP32-офсеты (неравномерный квант, потери на каждом хопе) и не double-single (нет выигрыша трафика): равномерная сетка, **точная распаковка** (int→double и 2⁻ᵏ точны), потеря = один rint/отправку — идеология B1; пакет 44 Б/атом против 80 (×1.8). FP32 — только парная математика (`LJDevF32`/`MorseDevF32`; геометрия/кат-офф/квантование — FP64, контракт §3.3 mixed-дока). **Апгрейд матрицы INV-9:** зона пакуется ровно раз за проход независимо от z ⇒ снап-последовательность z-независима ⇒ `production_mixed` **побитово и run-to-run, И «1 vs z»** (исходная оговорка матрицы относилась к отвергнутому FP32-формату; ZoneFSM §8.1/INV-9 обновлены). Тесты: `MixedRunToRunAnd1vsZBitwise` (два прогона + z∈{1,2,3} — побитово), `MixedAccuracyVsFp64` (max|dx| ≈ 1e-6 Å за 60 шагов — класс FP32-сил, транспорт не доминирует), `MixedTransportRangeHalts`; memcheck/racecheck чистые.
- **Критерий (главный):** те же тесты, что в M3.5 (побитовый детерминизм 1 стрим vs N стримов — в `deterministic_fp64`; run-to-run побитово при фиксированном z — в `production_mixed`; NVE-инвариант по методике M3.5; репликация §3.6; PBC; anti-deadlock) — на GPU; `compute-sanitizer` (memcheck+racecheck) чистый на всех `cuda`-тестах (локальный гейт `scripts/gpu_gate.sh`); achieved occupancy зафиксирована и сопоставлена с прогнозом M2.5/M2.7; **все перф-числа аннотированы режимом точности**; контрольная точка памяти: N=10⁷ влезает в ≤3.5 GiB.
- **Закрывает:** A3 (single-GPU = валидация логики, не производительности).
- **Соответствие ТЗ:** Спринт 3.

## M5a — Multi-process кольцо на одном GPU (MPI, локально)
- **Цель:** реальный `MpiTransport` и кольцо из ОС-процессов без потери корректности — максимум достоверности, достижимый на одной физической GPU (A4).
- **Делается:** `MpiTransport` с **host-staging** (D2H → MPI → H2D) как базовый путь — системный OpenMPI 4.1.6 не CUDA-aware (`opal_built_with_cuda_support=false`, измерено), device-указатели в MPI не ходят; контракт `ITransport` фиксирует memory space; 2–4 ранга × 1 GPU (MPS/Default compute mode); **k временных шагов на узел** (`decomposition.ring.steps_per_node`, Гл. 3.4, ур. 49–51) — главный рычаг occupancy (B4: в полёте у узла до k зон; ориентир k ≈ zones_to_saturate из зонда), трафик ↓ в k раз.
- **Критерий:** детерминизм/NVE-тесты M4 проходят на ≥2 MPI-рангах; насыщение GPU при k>1 зафиксировано профилем nsys; издержки host-staging измерены и внесены в Bench-отчёт.
- **Соответствие ТЗ:** Спринт 3 (ступень A4).

## M5b — Multi-GPU кластер (реальное кольцо)  [внешнее предусловие: доступ к узлу с ≥2 GPU]
- **Цель:** физическое кольцо без потери корректности; масштабирование.
- **Делается:** `P2PTransport` (`cudaMemcpyPeerAsync` — где P2P есть; NVLink/GPUDirect из базовых допущений исключены: на consumer-Blackwell их нет) и CUDA-aware MPI (UCX) за runtime-детектом `MPIX_Query_cuda_support`; перенос k-шагового кольца на физические узлы.
- **Критерий:** корректность (детерминизм/NVE) сохраняется на ≥2 GPU; метрика **P2P Masking %** ненулевая; **эффективность масштабирования $E = t_1/(P\,t_z)$ (операционализация ур. 47)** с числовым порогом, зафиксированным до прогона (ориентир диссертации — линейный рост, >95%, Гл. 2.6; недостижение = red flag метода на GPU-транспорте, не «график снят»); strong/weak scaling на LJ-конфигурациях масштаба §3.5 (10⁶–4·10⁶ атомов) — **флагманский приёмочный прогон** (визитка), 10⁶–10⁷ атомов = single-GPU фронтир 2025 (LAMMPS-KOKKOS 16 млн LJ/H100); полная спецификация — `_meta/VALIDATION_EXPERIMENT_2026-06-12.md` §6.
- **Соответствие ТЗ:** Спринт 3 + 5.

## M6 — Physics Suite (многочастичные потенциалы)
- **Цель:** за пределы Морзе.
- **Предусловие (поставить заранее, можно с M3):** локальный LAMMPS (сборка с GPU-пакетом не обязательна) — без него критерий RDF буквально неисполним; одноразовая сверка golden-датасета с `reference_data/in.al_morse` (лежит с M0 неиспользованным); генерация эталонных RDF/траекторий для больших N.
- **Делается:** EAM (отдельное ядро), затем FS; **дизайн партиального накопления электронной плотности ρ** поверх w-механизма зон (двухпроходность ρ→F на границах зон: w-вклад соседа — это и плотность, и силы; B-ревью, M6); **табличные потенциалы со сглаживанием (Гл. 1.3)** — заявлены в ТЗ §5 и трассируемости, вехи-владельца до сих пор не имели; интерфейс ML-потенциалов (заглушка). *(production mixed-precision перенесён в M4.)*
- **Критерий:** EAM-силы и RDF длинной траектории vs LAMMPS — совпадение RDF $\ge99.5\%$ (`Test_Statistical`) **по зафиксированной заранее спецификации метрики** (биннинг, $r_{max}$, норма сравнения — записать в Bench/ConfigSchema до прогона: метрика без спецификации нефальсифицируема); протокол приёмки — **энергия + дрейф импульса на наборе систем** (образец HOOMD/Anderson 2008, см. `MIXED_PRECISION_BESTPRACTICES_2026-06-11.md` §2); **точку плавления мерить ТОЛЬКО двухфазным coexistence** (`interface/NVE`, работает в нашем NVE-only режиме), НЕ одно-фазным нагревом (перегрев +19%) и НЕ на парном Морзе/LJ (металлическое плавление врут) — обоснование и протокол в `_meta/VALIDATION_EXPERIMENT_2026-06-12.md` §5,§7. EAM в `production_mixed` чувствителен к ошибке плотности ρ (embedding-функция) — закладывать FP64-плотность или `deterministic_fp64` (рекомендация LAMMPS для many-body). Замечание: EAM/FS — `[ENG]`, в диссертации не валидировались (Гл. 3.2 — лишь обоснование возможности).
- **Соответствие ТЗ:** Спринт 4.

## M7 — UX / I/O / DevOps / HPC-деплой
- **Цель:** продуктовая зрелость.
- **Делается:** Live CLI Dashboard (ANSI, P2P Masking %, инварианты), асинхронный I/O в фоновом потоке, Rescue Dump на CUDA-ошибки, `Apptainer.def` + `spack.yaml`, полный CI (линт, сборка GCC/Clang, юнит-тесты VerifyLab).
- **Критерий:** CI зелёный на чистой машине (без LAMMPS — golden-тесты); деплой контейнера на кластере проходит smoke-тест.
- **Соответствие ТЗ:** Спринт 1 (CI/контейнеры) + 5.

---

## Карта зависимостей

```
M0 ─► M1 ─► M2 ─► M2.5 ─► M2.6 ─► M2.7 ─► M3 ─► M3.5 ─► M4 ─► M5a ─► M5b ─► M6
                                            │     (M3.5 ∥ подготовка M4;      │
                                            │      гейт: критерий M4 — после  │
                                            │      зелёного M3.5)             │
                                            └─► M7 (частично ∥ M5–M6) ◄───────┘
```

## Сквозные требования (на каждой вехе)
- Каждый новый модуль — с юнит-тестом (см. `Skeleton_Interfaces §9`).
- `[ENG]`-решения не противоречат «источнику истины»; спорное — сверять с диссертацией.
- `seed` фиксирован; прогоны воспроизводимы.
- VerifyLab опционален за `-DWITH_LAMMPS=ON`; базовый CI — на golden-данных (A11).
- Локальная разработка — **Linux + RTX 5080** (sm_120, CUDA 13.1; ncu/nsys/compute-sanitizer локально; измеренный профиль — `_meta/ARCH_AUDIT_GPU_2026-06-11.md` §0). GPU-тесты несут метку `cuda` и гейтятся локально (`scripts/gpu_gate.sh`: ctest + compute-sanitizer); облачный CI — CPU-тесты + compile-only CUDA-job; self-hosted runner, если появится, — только `workflow_dispatch`/push в main (репо публичный: runner на `pull_request` = исполнение чужого кода на dev-машине). Кластер (≥2 GPU) — **внешнее предусловие только M5b**.
- Все перф-числа аннотируются режимом точности (`production_mixed`/`deterministic_fp64`): измеренный разрыв FP64:FP32 на целевом чипе — 1:66.7.

---
*Расширяет ТЗ §9. Критерии приёмки — из ТЗ §7 (VerifyLab) и эталонных данных. Ссылки на главы — к диссертации Андреева В.В. (2007).*
