# TD-MD Core — руководство проекта (для Claude Code)

GPU-движок молекулярной динамики на основе **метода декомпозиции времени (Time Decomposition, TD)** из кандидатской диссертации Андреева В.В. (2007). Цель: воспроизвести и развить подход диссертации — от подготовки структуры до запуска расчётов и анализа.

## Источник истины
1. `source/time_decomposition.docx` — диссертация (физика, метод, параметры). **При любом сомнении сверяйся с ней и ссылайся на разделы.** Для повседневной работы используй `source/time_decomposition.md` — полнотекстовую версию с **формулами, транскрибированными в LaTeX** (все 52 нумерованных уравнения; нерасшифрованные инлайн-символы помечены `⟨imageNNN⟩`). Старый `time_decomposition.txt` — сырой экстракт без формул, оставлен для grep-совместимости.
2. Сверка ключевых формул (ур. 33, 48, 62, K2, Морзе) с кодом выполнена 2026-06-11: `docs/_meta/FORMULA_VERIFICATION_2026-06-11.md`. Там же — маппинг обозначений (дисс. C1/C2/С1 ⇄ код `C_buf`/`C1`). Буквы зон приведены к диссертационным во всём проекте 2026-06-11.
3. `docs/` — проектные инструкции, выведенные из диссертации. Тег `[ENG]` помечает инженерные расширения (GPU/CUDA и т.п.), которых в диссертации нет и которые не были ею валидированы.

## КРИТИЧЕСКИЕ инварианты (нарушать нельзя)
- **Единицы:** только LAMMPS `metal` (эВ, Å, amu, ps). См. `docs/TD_MD_Core_Units_v1_0.md`.
- **Автомат зон:** цикл строго `o → d → w → c → p → o` (o=свободна, d=данные приняты, w=частичные силы, c=расчёт, p=готова к передаче) — **буквы строго по диссертации** (переименовано 2026-06-11; до этого проект использовал перестановку c/p/o — см. `FORMULA_VERIFICATION_2026-06-11.md` §6, историю не путать с текущей нотацией). Детали и инварианты — `docs/TD_MD_Core_ZoneFSM_v1_0.md`.
- **Причинность:** ни один атом не пересекает буфер $R_{buf}=v_{max}\Delta t C$ ($C\ge1$; ур. 33, в диссертации коэффициент назван C1, в конфиге — `C_buf`) за шаг; иначе HALT + rescue.
- **$\Delta t$/$v_{max}$ локальны** для узла — никаких глобальных `Allreduce` на каждом шаге (рушит конвейер).
- **Точность:** глобальные координаты FP64; накопление сил — **фикс-точечное int64 Q24.40** (B1/INV-9 ≡ AMBER SPFP: целочисленное сложение ассоциативно ⇒ детерминизм независимо от порядка потоков; FP64-`atomicAdd` — только референс сверки); ядра — `template<typename Real>` (одна кодовая база, FP32/FP64). Матрица гарантий детерминизма — `ZoneFSM §8/INV-9`; разбор смешанной точности vs другие движки и правки — `docs/_meta/MIXED_PRECISION_BESTPRACTICES_2026-06-11.md`.
- **Ансамбль v1:** только NVE. NVT/NPT — backlog.
- **Запрещено:** ReaxFF, PME, QEq (глобальная редукция несовместима с асинхронным TD).

## Карта документов (`docs/`)
- `TD_MD_Core_TZ_v1_1.md` — техническое задание (главный документ; Прил. А — трассируемость, Прил. Б — связи).
- `TD_MD_Core_ZoneFSM_v1_0.md` — формальный автомат зон (состояния, переходы, инварианты, псевдокод).
- `TD_MD_Core_Units_v1_0.md` — единицы и константы.
- `TD_MD_Core_ConfigSchema_v1_0.md` — схема `config.yaml`.
- `TD_MD_Core_Skeleton_Interfaces_v1_0.md` — раскладка репозитория и C++-интерфейсы.
- `TD_MD_Core_Roadmap_v1_0.md` — вехи M0–M7 с критериями приёмки.
- `TD_MD_Core_Glossary_v1_0.md` — терминология.
- `TD_MD_Core_TDfull_v1_1.md` — краткий пересказ диссертации; `TD_MD_Core_Rationale_v1_1.md` — обоснование решений.
- `TD_MD_Core_BRIEF_M0_v1_0.md` — стартовый бриф первой вехи.
- `docs/_meta/` — журнал решений (ревью и оценка архитектуры); это история «почему», **не** инструкция к сборке.

## Эталонные данные
`reference_data/` — золотой датасет Al/Морзе (72 атома, PBC), силы проверены конечными разностями. Самодостаточен: базовые тесты и CI **не требуют LAMMPS**. Используется в `Test_0_Step` и тесте детерминизма. N=72 — correctness-фикстура, **не** перф/физ-точка. Современная валидационная лестница (внешние эталоны NIST SRSW/LAMMPS/Мерц–Ширтс, флагман LJ 10⁶–10⁷, плавление через coexistence+EAM, реплики §3.5/§3.6) — `docs/_meta/VALIDATION_EXPERIMENT_2026-06-12.md`.

## Технологический стек
C++20, CUDA NVCC через слой HAL (без Kokkos; CUB/CCCL из тулкита разрешены — ТЗ §2.2), MPI, CMake, Google Test, YAML-cpp. Профилирование NVTX; деплой Apptainer/Spack (отложен до M5b/M7).

**Целевое железо (локально, с 2026-06-11):** RTX 5080 — sm_120, 84 SM, 1536 threads/SM, **24 blocks/SM**, 15.5 GiB; FP64:FP32 = **1:66.7**, `atomicAdd(double)` ~38× медленнее int64 (подтверждает B1). Измеренный профиль — `docs/_meta/ARCH_AUDIT_GPU_2026-06-11.md` §0. Тулчейн: CUDA 13.1, gcc 13.3, ncu/nsys/compute-sanitizer локально. Перф-числа всегда аннотировать режимом точности. Кластер ≥2 GPU — внешнее предусловие только M5b.

## Конвенции
- Пути в `config.yaml` — относительно корня репозитория (CWD движка), напр. `reference_data/al_fcc_72.data`.
- Кросс-ссылки между документами — по имени файла внутри `docs/`.
- `seed` фиксируется в конфиге; прогоны воспроизводимы.
- Каждый новый модуль — с юнит-тестом (см. `Skeleton_Interfaces §9`).
- VerifyLab/liblammps — опционально за CMake-флагом `-DWITH_LAMMPS=ON`; не делать жёсткой зависимостью CI.

## Текущий статус (2026-06-11)
Выполнены вехи **M0–M2.7** (критерии приёмки зелёные, см. `Roadmap`):
- **M0** — walking skeleton: CPU, O(N²) Морзе, velocity-Verlet NVE, `Test_0_Step` (силы ≤1e-6 эВ/Å vs golden) + `Test_NVE_Drift`.
- **M1** — `ZoneFSM` (чистая логика): таблица переходов, INV-3, seed S₁, чёт/нечёт I/O-порядок; `Test_Zone_FSM`.
- **M2** — буфер причинности (ур. 33) + авто-шаг C1/K2/C3 + HALT/rescue; `Test_Buffer`.
- **M2.5** — аналитический occupancy-зонд (Tier-1, без CUDA): отчёт `docs/_meta/occupancy_probe_2026-06-06.md`; вывод — одиночная тонкая зона недогружает GPU, нужен батчинг ~зон/стрим; `Test_Occupancy`.
- **M2.6** — тепловая инициализация (B8): `tdmd/core/thermal.hpp` (SplitMix64+Бокс—Мюллер — собственный ГПСЧ ради побитовой воспроизводимости, обнуление импульса ЦМ, точный рескейл к T на 3N−3); чтение `Velocities` в ридере; `run.init_temperature` в конфиге; `Test_Thermal` + NVE-дрейф/импульс при 300 K. T(0) точно, |p|≤2e-13, прогоны с одним seed побитово идентичны.
- **M2.7** — GPU bring-up: `tools/devprobe.cu` (измеренный профиль 5080 → `docs/_meta/gpu_baseline_2026-06-11.md`), зонд M2.5 подтверждён на железе (24 blocks/SM; отчёт `occupancy_probe_2026-06-11.md`, батч ≈9 зон устоял), CMake `TDMD_WITH_CUDA` + `Test_CUDA_Smoke` (метка `cuda`), `scripts/gpu_gate.sh` (ctest -L cuda + compute-sanitizer).

**M3 — ВЫПОЛНЕНА** (2026-06-12, критерии Tier-1 зелёные; полная летопись — `Roadmap`): гигиена B10 + генератор ГЦК; функтор `pair_morse.hpp`; `run_simulation()`; `Test_Determinism`; ядро вехи — `core/cluster.hpp` (Z-order, 32-атомные кластеры, full-neighbour список пар со skin) + `ClusteredMorse` (кросс-тест ≡O(N²) ~1e-15, fp32 1.4e-6, тайминг ×9.8 атомов → ×9.4 у кластерного vs ×89.9 у прямого); Tier-2 CUDA-прототип (56/62 рег, GPU≡CPU ≤5e-15, sanitizers чистые; ncu achieved-occupancy измерен 2026-06-12 после снятия RmProfilingAdminOnly: 6.5% при 75% теоретических — дефицит варпов в полёте, прогноз M2.5 подтверждён; `docs/_meta/ncu_occupancy_2026-06-12.md`). Закрытие 2026-06-12: **(а)** общий драйверный слой `pair_driver.hpp` (циклы O(N²)/кластерный — один источник; рефакторинг побитово прозрачен) + `pair_lj.hpp`/`lj.hpp` (`potential.type: lj`, `potential.lj{epsilon,sigma}`) + вириал `last_virial` в драйверах; **(б)** схемы обрезки `potential.truncation: cut|shift|force_shift` (`cutoff.hpp`; решение force-shift vs switching — `Rationale`; дефолт `shift`); **(в)** внешний эталон **NIST SRSW** — `Test_LJ_NIST`: 4 замороженные конфигурации × 3 схемы NIST, энергия+вириал в допуске печати (данные вербатим + таблица: `reference_data/nist_lj/README.md`); **(г)** формальные тесты кадрности перестройки vs C1 (баллистический — интервал ТОЧНО равен оценке; термический — нижняя граница; решётка при skin=1.0 не перестраивается — норма Верле-таблицы); **(д)** grid-куллинг списка пар: loose grid (растеризация AABB, CSR+stamp) ≡ O(C²) бит-в-бит, 10⁶ за 229 мс против 3.18 с (×14, вкл. сортировку), два отвергнутых варианта измерены — `Rationale`; **(е)** фикс INV-4: холодный старт больше не падает ложным HALT (буфер по прогнозу v_pred=v_max+a_max·Δt, [ENG]-уточнение ур.33 — `Rationale`/ZoneFSM §6; честные нарушения ловятся, тест переработан).

**M3.5 — ВЫПОЛНЕНА** (2026-06-12, оба PR; критерии приёмки зелёные — `Roadmap`): первый PR — B1 `core/fixed_accum.hpp` (int64 Q24.40/Q34.30, rint ≡ cvt.rni) + `core/zones.hpp` (w-механизм; `Test_Zones`: сумма w-вкладов ≡ монолит ПОБИТОВО, INV-8). Второй PR — **кольцо TimeConveyor-CPU**: `core/transport.hpp` (SPSC-каналы, CPU-заглушка `ITransport`) + `core/conveyor.hpp` (узел=`std::jthread`, проход=модельный шаг, зона уходит сразу после END; оркестровка `ZoneFSM` T1–T5 + seed §7.1, чёт/нечёт I/O §7.4; **Δt-handoff B2 позиционной Λ-цепочкой** — лаг светового конуса n−1 ⇒ Δt-последовательность побитово независима от z (ZoneFSM §6 п.3–5); **PBC-замыкание ротацией** (§7.2 [ENG]); INV-4 на END каждой зоны, стражи Overlap/StaleZone/NonFinite; членство зон статическое + честный страж — миграция в M4). `Test_Conveyor` (8 тестов): конвейер ≡ последовательному степперу побитово, **1 vs z∈{2..5} побитово (fixed и auto)**, PBC-замыкание+импульс, anti-deadlock z=1..6, HALT-кейсы, **реплика §3.6 — Al-72, свободные границы, авто-шаг, 25 900 шагов, 1 vs 4 узла — нулевое отклонение координат и скоростей**; `Test_NVE_Invariant` 50k (dt=1 фс, 300 K): многозонный **побитово ≡** 1-зонному, секулярный тренд −1.2e-2 kT/(нс·dof) [deterministic_fp64, morse+shift] — пол задан разрывом силы `shift` на R_cut (аудит C1), потолок 3× калиброван. Интеграция конвейера в CLI/config — в M4 (ключи `decomposition.ring` в ConfigSchema готовы).

**M4 — ВЫПОЛНЕНА** (2026-06-12, четыре PR; критерий приёмки зелёный целиком — `Roadmap`): **B1 на GPU** (`cuda/zone_force.cuh`: поквантовое округление вкладов ≡ `std::rint`, целочисленные частичные суммы, full-neighbour ≡ half-list побитово, sticky-переполнение; **LJ при `--fmad=false`: сырые int64-аккумуляторы CPU ≡ GPU ПОБИТОВО**) + интегрирование/редукции (`zone_integrate.cuh`, побитово ≡ CPU) + **GpuTimeConveyor** (`conveyor_gpu.cuh`: узел = `cudaStream_t` + хост-jthread, протокол M3.5 дословно; **StreamTransport**: заголовки — хост-SPSC, payload — `cudaMemcpyAsync` D2D + два кольца `cudaEvent_t` (arrival=INV-2, free=INV-5/6) + кредитная очередь слотов) + **production_mixed + РЕШЕНИЕ B5**: int32-фикс-офсеты (равномерный квант 2⁻ᵏ, точная распаковка, 44 Б/атом; FP32 — только парная математика) ⇒ **апгрейд INV-9: mixed побитово и run-to-run, и «1 vs z»**. GPU-приёмка: LJ fixed/auto GPU ≡ CPU-конвейеру и 1 vs N стримов побитово; PBC ≡ CPU; **реплика §3.6 на GPU 25 900 шагов 1 vs 4 — нулевое отклонение**; NVE 50k (тренд −2.9e-2 kT/(нс·dof) < потолка 3.7e-2); anti-deadlock z=1..5; gpu_gate (memcheck+racecheck) чистый. **Bench** (`tools/bench_conveyor.cu` + `TD_MD_Core_Bench_v1_0.md`): ring/baseline 0.60 (z=1, N=11k) → **4.04–4.07 (z=4, N=42.6k, fp64/mixed)**; mixed ≈ fp64 по скорости (узкое место — FP64-геометрия); occupancy: 32.7% (модель сходится точно), тонкие зоны 8.3% — прогноз M2.5 подтверждён; **память 10⁷: 2.18 GiB буферов (fp64, z=2/n=16) ✓**; CLI: `decomposition.*`/`ring.*` → CPU-эталонное кольцо (`config_ring.yaml`). Кросс-платформа Морзе — допусковая (квант-флипы exp ⇒ ляпуновская дивергенция за 26k шагов при внутриплатформенной побитовости). Рычаги пост-M4 (в `Bench`): порт пер-зонных списков пар на GPU-кольцо (абсолютные atom-steps/s), INV-7-сокращение слот-пула (×3 память).

**M5a — ВЫПОЛНЕНА** (2026-06-12; критерий зелёный — `Roadmap`): кольцо партиционировано по MPI-рангам (`RingPart`/`IBoundaryEdge` в `conveyor_gpu.cuh` — граница процессов = host-байты) + **`MpiRingEdge`** (host-staging D2H→`MPI_Isend`(пул 4)→H2D, не CUDA-aware OpenMPI; HALT-яд с TTL; `MPI_THREAD_MULTIPLE`); **k шагов на узел (ур. 49–51) редукцией: k логических узлов на ранг** — трафик /k по построению, траектория побитово не зависит от k. `Test_MPI_Conveyor` (np∈{2,3,4} × k∈{1,2,3} × fixed/auto/mixed): **MPI-кольцо побитово ≡ одно-процессному**, включая реплику §3.6 на np=2×k=2=Z=4 (4 процессора диссертации, 25 900 авто-шагов, нулевое отклонение); насыщение k>1 — nsys (ядра ~const, wall −40%); host-staging ×1.95→×1.32 (k=4) — Bench. Сборка: `-DTDMD_WITH_CUDA=ON -DTDMD_WITH_MPI=ON`; `ctest -L mpi` (np=2/np=4).

✅ **Пост-M5a (2026-06-12): порт списков пар на GPU-кольцо** — `cuda/zone_cells.cuh` (пер-зонные cell-list'ы каждый проход, CUB-скан; побитовая прозрачность по построению — B1-куллинг меняет работу, не результат; A/B cells≡tiles + фаззинг). **Флагман 10⁶ атомов: 2.06e7 atom-steps/s fp64, 2.33e7 mixed** (×21.8 от куллинга, ×86.8 к наивному baseline на 42.6k); память 10⁷ — 3.08 GiB ✓. Включён по умолчанию (`ConveyorOptions.cell_lists`).

**M5b — ОТЛОЖЕННАЯ/ПАРАЛЛЕЛЬНАЯ веха** (multi-GPU кластер; внешний триггер: узел с ≥2 GPU; критерий прогоняется немедленно при появлении ресурса и до заявлений уровня M7 — исключение в правиле ворот, `Roadmap`). **Текущая веха — M6** (Physics Suite: EAM/многочастичные). **Предусловие LAMMPS ВЫПОЛНЕНО (2026-06-14):** локальный LAMMPS (stable 22Jul2025, MANYBODY/`eam/alloy`) уже собран в соседних проектах (`/home/slava8519/tdmd_V2/verify/third_party/lammps/build_tdmd/lmp`) + реальные Al-setfl — apt не нужен; путь в памяти (`lammps-local-install`). Дизайн+ход M6 — `docs/_meta/M6_EAM_MANYBODY_DESIGN_2026-06-14.md` (состязательно верифицирован). **M6 EAM-лестница ВЫПОЛНЕНА** (ветка `opus`, каждый PR: состязательный дизайн для крупных + приёмка после; парный `TimeConveyor` всё время байт-нетронут): **E0** дескриптор `IManyBodyPotential`+гео-стражи; **E1** аналитический FS-EAM+FD; **E2** сплайн-setfl с репликацией LAMMPS `interpolate()` (сверка `eam/alloy run 0` ~1e-12 эВ/Å, эталон заморожен в `reference_data/eam_al/`); **E3** serial zone-EAM (трёхзонная резиденция `{S_{i-1},S_i,S_{i+1}}`, full-neighbour≡Newton-3, побитовый оракул кольца); **E3b** threaded `EamRing` (`eam_ring.hpp`, free-z, second-forward-hop=задержка-на-зону) + **E3b-PBC** (циклическое окно+defer_head, tail-batched отправки); **E4** NVE energy-conservation; **E5** GPU EAM-ядра (`cuda/zone_eam.cuh`, CPU↔GPU **побитово** Q19.44/Q23.40, структурный `--fmad=false` + on-device near-knot flag-audit — закрывает OQ1-на-GPU и E2-P2; memcheck+racecheck чисто); **E7** RDF g(r)-пики vs замороженный LAMMPS. **Отложено** (не блокирует): GPU-ring интеграция (E5b), melt-coexistence (длинный эксперимент), MEAM/Tersoff/ReaxFF/ML (поле `needs_transpose` заложено, гейты MB1/MB2). **Формальный Physical-Oracle/E6 ВЫПОЛНЕН** как Gate-02a (см. M4-N ниже; EAM-специфичный интент по-прежнему покрыт `ForwardOnlyWindowDiverges`/`PbcCyclicWindowVsFp64Oracle`/GPU-bitwise).

**M4-N ВЫПОЛНЕН (2026-06-17, перф-трек, ветка `opus`; каждый кусок — состязательный дизайн ДО кода + приёмка ПОСЛЕ; полная летопись — `TD_MD_Core_Bench_v1_0.md`):** (а) **GPU-EAM ms/step-разбивка** density/embedding/force + hit-rate + occupancy (`metrics/eam_breakdown.hpp`, `tools/bench_eam.cu`; hit-rate 0.3–1.4% ⇒ cell-list возможность ×72–333, растёт ∝N — количественный вход бейкоффа); (б) **K-метрики** `K_eff`/`K_pred` свип равновесие→удар (`metrics/k_cadence.hpp`, `tools/bench_kcadence.cpp`; `K_pred=skin/(2·mean(R_buf))` гармонически-консистентно; conservatism 7.98 холод→1.60 удар→C_buf=1.5 — фактор амортизации PersistentVerlet; BUG-1 арифм-vs-гармон. среднее исправлен приёмкой); (в) **Gate-02a Physical Oracle** (`tests/test_oracle.cpp`/`Test_Physical_Oracle`) — superset⇒bitwise гейт против молча выпавшей пары (к чему 1-vs-z и run-to-run слепы): REFERENCE=`direct_pair_loop`, PERSISTENT=то же тело по `nbr[i]`; A1 `memcmp`+A2 счётчик пропусков; позитив 5000 шагов bit-в-bit (218 перестроек, переиспользование ~23 шага ≡ `K_eff`~22 — две части M4-N кросс-валидируются), зубы ловят грубый триггер (`max|ΔF|`~0.16 эВ/Å), статический lock. **M4-S/M4-B ВЫПОЛНЕНЫ (2026-06-17, состязательный дизайн+приёмка; ветка `opus`):** **M4-S** — скин-критерий на Λ-цепочке был построен (PR-0..PR-4: charge=`2·R_buf`, K_on/K_off гистерезис, broadcast NL-INV-4, PR-3 hybrid, PR-4 drift L2a); закрыт **off-equilibrium валидацией** `VerletHybridOffEquilibriumStress1vsZ` (verlet K>1≡cells + 1-vs-z bitwise + по-проходная pe, 3000 шагов 2500K+shock, **lag L=7**, memcheck чисто); **И2** (`d_(1)+d_(2)` vs `2·d_max`) состязательно ОТЛОЖЕН (выигрыш ~0 при L≫1, `verlet_skin/ROADMAP`). **M4-B** — бейкофф `bench_conveyor` cells/verlet/tiles (measure-first): cold **1.64×** verlet→warm 1.22×→shock 1.05×; tiles ×30 медленнее cells (куллинг обязателен); skin=1.0 sweet spot. **РЕШЕНИЕ: default=cells (memory-gated: cross-role CSR ~9ГБ@1e7), verlet — opt-in лёвер, D K-aware (`K_on=3.0`/`K_off=1.5`, НЕ ≈2)**; конфиг `neighbor.verlet.*` (GPU-ring; CPU-CLI игнорирует+warns). **Gate-02b SATISFIED** (транзитивно verlet-K>1≡cells≡tiles + по-проходная pe; декод int64→double lossless в диапазоне ⇒ double-bitwise⟺raw-int64). Числа/решение — `TD_MD_Core_Bench_v1_0.md §M4-B`. **Отложено** (measure-first): TileMask (backend C), EAM-через-ринг бейкофф (E5b), пер-фазный stacked-bar, И-B/И-C, cross-idx память (блокирует verlet-default). Дальше — на выбор: E5b (EAM на GPU-кольцо + бейкофф на EAM), или TileMask, или другой трек. ✅ Пост-M5a (2026-06-12): **INV-7-пул + потоковая P₁** — закон ёмкости Z·S ≥ n+слак (найден встречным дедлоком; ≡ дисс. «≥6 зон/процессор»), S = min(n+2, max(8, ⌈n/Z⌉+4)); память 10⁷: z=4/n=16 4.36→2.15 GiB, z=4/n=32 → 1.63 GiB (×2.6); перф нетронут. ✅ Пост-M5a (2026-06-13): **FMA-сборка** (`bench_conveyor_fma`: +4–7%, 1M mixed 2.50e7 a-st/s; `--verify-det`: GPU-детерминизм при FMA ПОБИТОВО — эмпирика под B1) + **NVTX** (`TDMD_WITH_NVTX`; nsys-диагноз: end+sync 32.6% — пер-зонная D2H+sync END-скаляров = следующая цель оптимизации). ✅ Пост-M5a (2026-06-13): **батчинг END-sync** — один sync/проход вместо n (стражи реплеем перед последней отправкой; лемма: проваленный проход не шлёт последнюю зону ⇒ префикс stats цел; побитово прозрачно); end+sync 32.6→12.1%, NVE 50k −10%; новый лидер flush = межстримовый idle (рычаг M5b). Локальные рычаги исчерпаны. См. `Roadmap` M5b/M6. Переход к следующей вехе — только после зелёного критерия текущей; вехи с внешним триггером (M5b) — отложенные/параллельные и не блокируют (`Roadmap`, правило ворот). Сборка: `cmake -S . -B build -G Ninja && cmake --build build && ctest --test-dir build` (22 CPU-теста: +M6 `Test_ManyBody_Config`/`Test_EAM`/`Test_EAM_Spline`/`Test_EAM_Zone`/`Test_EAM_Ring`/`Test_EAM_RDF`; +M4-N `Test_Metrics`/`Test_Physical_Oracle`; CUDA +`Test_CUDA_EAM`); CUDA-вариант: `cmake -S . -B build-cuda -G Ninja -DTDMD_WITH_CUDA=ON && cmake --build build-cuda && ./scripts/gpu_gate.sh build-cuda`. Python-тулинг (numpy для `tools/gen_fcc.py`): системного pip нет — `python3 -m venv .venv && .venv/bin/pip install numpy` (каталог `.venv` в .gitignore).
