# TD-MD Core — руководство проекта (для Claude Code)

GPU-движок молекулярной динамики на основе **метода декомпозиции времени (Time Decomposition, TD)** из кандидатской диссертации Андреева В.В. (2007). Цель: воспроизвести и развить подход диссертации — от подготовки структуры до запуска расчётов и анализа.

## Источник истины
1. `source/time_decomposition.docx` — диссертация (физика, метод, параметры). **При любом сомнении сверяйся с ней и ссылайся на разделы.** Для повседневной работы используй `source/time_decomposition.md` — полнотекстовую версию с **формулами, транскрибированными в LaTeX** (все 52 нумерованных уравнения; нерасшифрованные инлайн-символы помечены `⟨imageNNN⟩`). Старый `time_decomposition.txt` — сырой экстракт без формул, оставлен для grep-совместимости.
2. Сверка ключевых формул (ур. 33, 48, 62, K2, Морзе) с кодом выполнена 2026-06-11: `docs/_meta/FORMULA_VERIFICATION_2026-06-11.md`. Там же — маппинг обозначений (дисс. C1/C2/С1 ⇄ код `C_buf`/`C1`). Буквы зон приведены к диссертационным во всём проекте 2026-06-11.
3. `docs/` — проектные инструкции, выведенные из диссертации. Тег `[ENG]` помечает инженерные расширения (GPU/CUDA и т.п.), которых в диссертации нет и которые не были ею валидированы.

## КРИТИЧЕСКИЕ инварианты (нарушать нельзя)
- **Единицы:** только LAMMPS `metal` (эВ, Å, amu, ps). См. `docs/TD_MD_Core_Units_v1_0.md`.
- **Автомат зон:** цикл строго `o → d → w → c → p → o` (o=свободна, d=данные приняты, w=частичные силы, c=расчёт, p=готова к передаче) — **буквы строго по диссертации** (переименовано 2026-06-11; до этого проект использовал перестановку c/p/o — см. `FORMULA_VERIFICATION_2026-06-11.md` §6, историю не путать с текущей нотацией). Детали и инварианты — `docs/TD_MD_Core_ZoneFSM_v1_0.md`.
- **Причинность:** ни один атом не пересекает буфер $R_{buf}=v_{max}\Delta t C$ ($C\ge1$; ур. 33, в диссертации коэффициент назван C1, в конфиге — `C_buf`) за шаг; иначе HALT + rescue.
- **$\Delta t$/$v_{max}$ локальны** для узла — никаких глобальных `Allreduce` на каждом шаге (рушит конвейер).
- **Точность:** глобальные координаты FP64; накопление сил — **фикс-точечное int64 Q24.40** (B1/INV-9 ≡ AMBER SPFP: целочисленное сложение ассоциативно ⇒ детерминизм независимо от порядка потоков; FP64-`atomicAdd` — только референс сверки); ядра — `template<typename Real>` (одна кодовая база, FP32/FP64). Матрица гарантий детерминизма — `ZoneFSM §8/INV-9`; разбор смешанной точности vs другие движки и правки — `docs/_meta/MIXED_PRECISION_BESTPRACTICES_2026-06-11.md`.
- **Ансамбль v1:** только NVE. NVT/NPT — backlog.
- **Запрещено:** ReaxFF, PME, QEq (глобальная редукция несовместима с асинхронным TD).

## Карта документов (`docs/`)
- `TD_MD_Core_TZ_v1_1.md` — техническое задание (главный документ; Прил. А — трассируемость, Прил. Б — связи).
- `TD_MD_Core_ZoneFSM_v1_0.md` — формальный автомат зон (состояния, переходы, инварианты, псевдокод).
- `TD_MD_Core_Units_v1_0.md` — единицы и константы.
- `TD_MD_Core_ConfigSchema_v1_0.md` — схема `config.yaml`.
- `TD_MD_Core_Skeleton_Interfaces_v1_0.md` — раскладка репозитория и C++-интерфейсы.
- `TD_MD_Core_Roadmap_v1_0.md` — вехи M0–M7 с критериями приёмки.
- `TD_MD_Core_Glossary_v1_0.md` — терминология.
- `TD_MD_Core_TDfull_v1_1.md` — краткий пересказ диссертации; `TD_MD_Core_Rationale_v1_1.md` — обоснование решений.
- `TD_MD_Core_BRIEF_M0_v1_0.md` — стартовый бриф первой вехи.
- `docs/_meta/` — журнал решений (ревью и оценка архитектуры); это история «почему», **не** инструкция к сборке.

## Эталонные данные
`reference_data/` — золотой датасет Al/Морзе (72 атома, PBC), силы проверены конечными разностями. Самодостаточен: базовые тесты и CI **не требуют LAMMPS**. Используется в `Test_0_Step` и тесте детерминизма. N=72 — correctness-фикстура, **не** перф/физ-точка. Современная валидационная лестница (внешние эталоны NIST SRSW/LAMMPS/Мерц–Ширтс, флагман LJ 10⁶–10⁷, плавление через coexistence+EAM, реплики §3.5/§3.6) — `docs/_meta/VALIDATION_EXPERIMENT_2026-06-12.md`.

## Технологический стек
C++20, CUDA NVCC через слой HAL (без Kokkos; CUB/CCCL из тулкита разрешены — ТЗ §2.2), MPI, CMake, Google Test, YAML-cpp. Профилирование NVTX; деплой Apptainer/Spack (отложен до M5b/M7).

**Целевое железо (локально, с 2026-06-11):** RTX 5080 — sm_120, 84 SM, 1536 threads/SM, **24 blocks/SM**, 15.5 GiB; FP64:FP32 = **1:66.7**, `atomicAdd(double)` ~38× медленнее int64 (подтверждает B1). Измеренный профиль — `docs/_meta/ARCH_AUDIT_GPU_2026-06-11.md` §0. Тулчейн: CUDA 13.1, gcc 13.3, ncu/nsys/compute-sanitizer локально. Перф-числа всегда аннотировать режимом точности. Кластер ≥2 GPU — внешнее предусловие только M5b.

## Конвенции
- Пути в `config.yaml` — относительно корня репозитория (CWD движка), напр. `reference_data/al_fcc_72.data`.
- Кросс-ссылки между документами — по имени файла внутри `docs/`.
- `seed` фиксируется в конфиге; прогоны воспроизводимы.
- Каждый новый модуль — с юнит-тестом (см. `Skeleton_Interfaces §9`).
- LAMMPS — НЕ зависимость сборки/CI (флага `-DWITH_LAMMPS` НЕ существует — фантом ранних доков, снят 2026-07-02): эталоны заморожены в `reference_data/` с `gen_*.in`-скриптами; живой LAMMPS нужен только офлайн для генерации новых эталонов (локальный бинарь — память `lammps-local-install`). Секция `verify` конфига — [INERT] (ConfigSchema §1/§5).

## Текущий статус (2026-06-11)
Выполнены вехи **M0–M2.7** (критерии приёмки зелёные, см. `Roadmap`):
- **M0** — walking skeleton: CPU, O(N²) Морзе, velocity-Verlet NVE, `Test_0_Step` (силы ≤1e-6 эВ/Å vs golden) + `Test_NVE_Drift`.
- **M1** — `ZoneFSM` (чистая логика): таблица переходов, INV-3, seed S₁, чёт/нечёт I/O-порядок; `Test_Zone_FSM`.
- **M2** — буфер причинности (ур. 33) + авто-шаг C1/K2/C3 + HALT/rescue; `Test_Buffer`.
- **M2.5** — аналитический occupancy-зонд (Tier-1, без CUDA): отчёт `docs/_meta/occupancy_probe_2026-06-06.md`; вывод — одиночная тонкая зона недогружает GPU, нужен батчинг ~зон/стрим; `Test_Occupancy`.
- **M2.6** — тепловая инициализация (B8): `tdmd/core/thermal.hpp` (SplitMix64+Бокс—Мюллер — собственный ГПСЧ ради побитовой воспроизводимости, обнуление импульса ЦМ, точный рескейл к T на 3N−3); чтение `Velocities` в ридере; `run.init_temperature` в конфиге; `Test_Thermal` + NVE-дрейф/импульс при 300 K. T(0) точно, |p|≤2e-13, прогоны с одним seed побитово идентичны.
- **M2.7** — GPU bring-up: `tools/devprobe.cu` (измеренный профиль 5080 → `docs/_meta/gpu_baseline_2026-06-11.md`), зонд M2.5 подтверждён на железе (24 blocks/SM; отчёт `occupancy_probe_2026-06-11.md`, батч ≈9 зон устоял), CMake `TDMD_WITH_CUDA` + `Test_CUDA_Smoke` (метка `cuda`), `scripts/gpu_gate.sh` (ctest -L cuda + compute-sanitizer).

**M3 — ВЫПОЛНЕНА** (2026-06-12, критерии Tier-1 зелёные; полная летопись — `Roadmap`): гигиена B10 + генератор ГЦК; функтор `pair_morse.hpp`; `run_simulation()`; `Test_Determinism`; ядро вехи — `core/cluster.hpp` (Z-order, 32-атомные кластеры, full-neighbour список пар со skin) + `ClusteredMorse` (кросс-тест ≡O(N²) ~1e-15, fp32 1.4e-6, тайминг ×9.8 атомов → ×9.4 у кластерного vs ×89.9 у прямого); Tier-2 CUDA-прототип (56/62 рег, GPU≡CPU ≤5e-15, sanitizers чистые; ncu achieved-occupancy измерен 2026-06-12 после снятия RmProfilingAdminOnly: 6.5% при 75% теоретических — дефицит варпов в полёте, прогноз M2.5 подтверждён; `docs/_meta/ncu_occupancy_2026-06-12.md`). Закрытие 2026-06-12: **(а)** общий драйверный слой `pair_driver.hpp` (циклы O(N²)/кластерный — один источник; рефакторинг побитово прозрачен) + `pair_lj.hpp`/`lj.hpp` (`potential.type: lj`, `potential.lj{epsilon,sigma}`) + вириал `last_virial` в драйверах; **(б)** схемы обрезки `potential.truncation: cut|shift|force_shift` (`cutoff.hpp`; решение force-shift vs switching — `Rationale`; дефолт `shift`); **(в)** внешний эталон **NIST SRSW** — `Test_LJ_NIST`: 4 замороженные конфигурации × 3 схемы NIST, энергия+вириал в допуске печати (данные вербатим + таблица: `reference_data/nist_lj/README.md`); **(г)** формальные тесты кадрности перестройки vs C1 (баллистический — интервал ТОЧНО равен оценке; термический — нижняя граница; решётка при skin=1.0 не перестраивается — норма Верле-таблицы); **(д)** grid-куллинг списка пар: loose grid (растеризация AABB, CSR+stamp) ≡ O(C²) бит-в-бит, 10⁶ за 229 мс против 3.18 с (×14, вкл. сортировку), два отвергнутых варианта измерены — `Rationale`; **(е)** фикс INV-4: холодный старт больше не падает ложным HALT (буфер по прогнозу v_pred=v_max+a_max·Δt, [ENG]-уточнение ур.33 — `Rationale`/ZoneFSM §6; честные нарушения ловятся, тест переработан).

**M3.5 — ВЫПОЛНЕНА** (2026-06-12, оба PR; критерии приёмки зелёные — `Roadmap`): первый PR — B1 `core/fixed_accum.hpp` (int64 Q24.40/Q34.30, rint ≡ cvt.rni) + `core/zones.hpp` (w-механизм; `Test_Zones`: сумма w-вкладов ≡ монолит ПОБИТОВО, INV-8). Второй PR — **кольцо TimeConveyor-CPU**: `core/transport.hpp` (SPSC-каналы, CPU-заглушка `ITransport`) + `core/conveyor.hpp` (узел=`std::jthread`, проход=модельный шаг, зона уходит сразу после END; оркестровка `ZoneFSM` T1–T5 + seed §7.1, чёт/нечёт I/O §7.4; **Δt-handoff B2 позиционной Λ-цепочкой** — лаг светового конуса n−1 ⇒ Δt-последовательность побитово независима от z (ZoneFSM §6 п.3–5); **PBC-замыкание ротацией** (§7.2 [ENG]); INV-4 на END каждой зоны, стражи Overlap/StaleZone/NonFinite; членство зон статическое + честный страж — миграция в M4). `Test_Conveyor` (8 тестов): конвейер ≡ последовательному степперу побитово, **1 vs z∈{2..5} побитово (fixed и auto)**, PBC-замыкание+импульс, anti-deadlock z=1..6, HALT-кейсы, **реплика §3.6 — Al-72, свободные границы, авто-шаг, 25 900 шагов, 1 vs 4 узла — нулевое отклонение координат и скоростей**; `Test_NVE_Invariant` 50k (dt=1 фс, 300 K): многозонный **побитово ≡** 1-зонному, секулярный тренд −1.2e-2 kT/(нс·dof) [deterministic_fp64, morse+shift] — пол задан разрывом силы `shift` на R_cut (аудит C1), потолок 3× калиброван. Интеграция конвейера в CLI/config — в M4 (ключи `decomposition.ring` в ConfigSchema готовы).

**M4 — ВЫПОЛНЕНА** (2026-06-12, четыре PR; критерий приёмки зелёный целиком — `Roadmap`): **B1 на GPU** (`cuda/zone_force.cuh`: поквантовое округление вкладов ≡ `std::rint`, целочисленные частичные суммы, full-neighbour ≡ half-list побитово, sticky-переполнение; **LJ при `--fmad=false`: сырые int64-аккумуляторы CPU ≡ GPU ПОБИТОВО**) + интегрирование/редукции (`zone_integrate.cuh`, побитово ≡ CPU) + **GpuTimeConveyor** (`conveyor_gpu.cuh`: узел = `cudaStream_t` + хост-jthread, протокол M3.5 дословно; **StreamTransport**: заголовки — хост-SPSC, payload — `cudaMemcpyAsync` D2D + два кольца `cudaEvent_t` (arrival=INV-2, free=INV-5/6) + кредитная очередь слотов) + **production_mixed + РЕШЕНИЕ B5**: int32-фикс-офсеты (равномерный квант 2⁻ᵏ, точная распаковка, 44 Б/атом; FP32 — только парная математика) ⇒ **апгрейд INV-9: mixed побитово и run-to-run, и «1 vs z»**. GPU-приёмка: LJ fixed/auto GPU ≡ CPU-конвейеру и 1 vs N стримов побитово; PBC ≡ CPU; **реплика §3.6 на GPU 25 900 шагов 1 vs 4 — нулевое отклонение**; NVE 50k (тренд −2.9e-2 kT/(нс·dof) < потолка 3.7e-2); anti-deadlock z=1..5; gpu_gate (memcheck+racecheck) чистый. **Bench** (`tools/bench_conveyor.cu` + `TD_MD_Core_Bench_v1_0.md`): ring/baseline 0.60 (z=1, N=11k) → **4.04–4.07 (z=4, N=42.6k, fp64/mixed)**; mixed ≈ fp64 по скорости (узкое место — FP64-геометрия); occupancy: 32.7% (модель сходится точно), тонкие зоны 8.3% — прогноз M2.5 подтверждён; **память 10⁷: 2.18 GiB буферов (fp64, z=2/n=16) ✓**; CLI: `decomposition.*`/`ring.*` → CPU-эталонное кольцо (`config_ring.yaml`). Кросс-платформа Морзе — допусковая (квант-флипы exp ⇒ ляпуновская дивергенция за 26k шагов при внутриплатформенной побитовости). Рычаги пост-M4 (в `Bench`): порт пер-зонных списков пар на GPU-кольцо (абсолютные atom-steps/s), INV-7-сокращение слот-пула (×3 память).

**M5a — ВЫПОЛНЕНА** (2026-06-12; критерий зелёный — `Roadmap`): кольцо партиционировано по MPI-рангам (`RingPart`/`IBoundaryEdge` в `conveyor_gpu.cuh` — граница процессов = host-байты) + **`MpiRingEdge`** (host-staging D2H→`MPI_Isend`(пул 4)→H2D, не CUDA-aware OpenMPI; HALT-яд с TTL; `MPI_THREAD_MULTIPLE`); **k шагов на узел (ур. 49–51) редукцией: k логических узлов на ранг** — трафик /k по построению, траектория побитово не зависит от k. `Test_MPI_Conveyor` (np∈{2,3,4} × k∈{1,2,3} × fixed/auto/mixed): **MPI-кольцо побитово ≡ одно-процессному**, включая реплику §3.6 на np=2×k=2=Z=4 (4 процессора диссертации, 25 900 авто-шагов, нулевое отклонение); насыщение k>1 — nsys (ядра ~const, wall −40%); host-staging ×1.95→×1.32 (k=4) — Bench. Сборка: `-DTDMD_WITH_CUDA=ON -DTDMD_WITH_MPI=ON`; `ctest -L mpi` (np=2/np=4).

✅ **Пост-M5a (2026-06-12): порт списков пар на GPU-кольцо** — `cuda/zone_cells.cuh` (пер-зонные cell-list'ы каждый проход, CUB-скан; побитовая прозрачность по построению — B1-куллинг меняет работу, не результат; A/B cells≡tiles + фаззинг). **Флагман 10⁶ атомов: 2.06e7 atom-steps/s fp64, 2.33e7 mixed** (×21.8 от куллинга, ×86.8 к наивному baseline на 42.6k); память 10⁷ — 3.08 GiB ✓. Включён по умолчанию (`ConveyorOptions.cell_lists`).

**M5b — ОТЛОЖЕННАЯ/ПАРАЛЛЕЛЬНАЯ веха** (multi-GPU кластер; внешний триггер: узел с ≥2 GPU; критерий прогоняется немедленно при появлении ресурса и до заявлений уровня M7 — исключение в правиле ворот, `Roadmap`). **Текущая веха — M6** (Physics Suite: EAM/многочастичные). **Предусловие LAMMPS ВЫПОЛНЕНО (2026-06-14):** локальный LAMMPS (stable 22Jul2025, MANYBODY/`eam/alloy`) уже собран в соседних проектах (`/home/slava8519/tdmd_V2/verify/third_party/lammps/build_tdmd/lmp`) + реальные Al-setfl — apt не нужен; путь в памяти (`lammps-local-install`). Дизайн+ход M6 — `docs/_meta/M6_EAM_MANYBODY_DESIGN_2026-06-14.md` (состязательно верифицирован). **M6 EAM-лестница ВЫПОЛНЕНА** (ветка `opus`, каждый PR: состязательный дизайн для крупных + приёмка после; парный `TimeConveyor` всё время байт-нетронут): **E0** дескриптор `IManyBodyPotential`+гео-стражи; **E1** аналитический FS-EAM+FD; **E2** сплайн-setfl с репликацией LAMMPS `interpolate()` (сверка `eam/alloy run 0` ~1e-12 эВ/Å, эталон заморожен в `reference_data/eam_al/`); **E3** serial zone-EAM (трёхзонная резиденция `{S_{i-1},S_i,S_{i+1}}`, full-neighbour≡Newton-3, побитовый оракул кольца); **E3b** threaded `EamRing` (`eam_ring.hpp`, free-z, second-forward-hop=задержка-на-зону) + **E3b-PBC** (циклическое окно+defer_head, tail-batched отправки); **E4** NVE energy-conservation; **E5** GPU EAM-ядра (`cuda/zone_eam.cuh`, CPU↔GPU **побитово** Q19.44/Q23.40, структурный `--fmad=false` + on-device near-knot flag-audit — закрывает OQ1-на-GPU и E2-P2; memcheck+racecheck чисто); **E7** RDF g(r)-пики vs замороженный LAMMPS. **Отложено** (не блокирует): GPU-ring интеграция (E5b), melt-coexistence (длинный эксперимент), MEAM/Tersoff/ReaxFF/ML (поле `needs_transpose` заложено, гейты MB1/MB2). **Формальный Physical-Oracle/E6 ВЫПОЛНЕН** как Gate-02a (см. M4-N ниже; EAM-специфичный интент по-прежнему покрыт `ForwardOnlyWindowDiverges`/`PbcCyclicWindowVsFp64Oracle`/GPU-bitwise).

**M4-N ВЫПОЛНЕН (2026-06-17, перф-трек, ветка `opus`; каждый кусок — состязательный дизайн ДО кода + приёмка ПОСЛЕ; полная летопись — `TD_MD_Core_Bench_v1_0.md`):** (а) **GPU-EAM ms/step-разбивка** density/embedding/force + hit-rate + occupancy (`metrics/eam_breakdown.hpp`, `tools/bench_eam.cu`; hit-rate 0.3–1.4% ⇒ cell-list возможность ×72–333, растёт ∝N — количественный вход бейкоффа); (б) **K-метрики** `K_eff`/`K_pred` свип равновесие→удар (`metrics/k_cadence.hpp`, `tools/bench_kcadence.cpp`; `K_pred=skin/(2·mean(R_buf))` гармонически-консистентно; conservatism 7.98 холод→1.60 удар→C_buf=1.5 — фактор амортизации PersistentVerlet; BUG-1 арифм-vs-гармон. среднее исправлен приёмкой); (в) **Gate-02a Physical Oracle** (`tests/test_oracle.cpp`/`Test_Physical_Oracle`) — superset⇒bitwise гейт против молча выпавшей пары (к чему 1-vs-z и run-to-run слепы): REFERENCE=`direct_pair_loop`, PERSISTENT=то же тело по `nbr[i]`; A1 `memcmp`+A2 счётчик пропусков; позитив 5000 шагов bit-в-bit (218 перестроек, переиспользование ~23 шага ≡ `K_eff`~22 — две части M4-N кросс-валидируются), зубы ловят грубый триггер (`max|ΔF|`~0.16 эВ/Å), статический lock. **M4-S/M4-B ВЫПОЛНЕНЫ (2026-06-17, состязательный дизайн+приёмка; ветка `opus`):** **M4-S** — скин-критерий на Λ-цепочке был построен (PR-0..PR-4: charge=`2·R_buf`, K_on/K_off гистерезис, broadcast NL-INV-4, PR-3 hybrid, PR-4 drift L2a); закрыт **off-equilibrium валидацией** `VerletHybridOffEquilibriumStress1vsZ` (verlet K>1≡cells + 1-vs-z bitwise + по-проходная pe, 3000 шагов 2500K+shock, **lag L=7**, memcheck чисто); **И2** (`d_(1)+d_(2)` vs `2·d_max`) состязательно ОТЛОЖЕН (выигрыш ~0 при L≫1, `verlet_skin/ROADMAP`). **M4-B** — бейкофф `bench_conveyor` cells/verlet/tiles (measure-first): cold **1.64×** verlet→warm 1.22×→shock 1.05×; tiles ×30 медленнее cells (куллинг обязателен); skin=1.0 sweet spot. **РЕШЕНИЕ: default=cells (memory-gated: cross-role CSR ~9ГБ@1e7), verlet — opt-in лёвер, D K-aware (`K_on=3.0`/`K_off=1.5`, НЕ ≈2)**; конфиг `neighbor.verlet.*` (GPU-ring; CPU-CLI игнорирует+warns). **Gate-02b SATISFIED** (транзитивно verlet-K>1≡cells≡tiles + по-проходная pe; декод int64→double lossless в диапазоне ⇒ double-bitwise⟺raw-int64). Числа/решение — `TD_MD_Core_Bench_v1_0.md §M4-B`. **Отложено** (measure-first): TileMask (backend C), пер-фазный stacked-bar, И-B/И-C, cross-idx память (блокирует verlet-default).

**E5b ВЫПОЛНЕН (2026-06-17, состязательный дизайн ДО + приёмка ПОСЛЕ; ветка `opus`; дизайн — `_meta/E5B_GPU_EAM_RING_DESIGN_2026-06-17.md`):** EAM на GPU-кольце. Центр проблемы: пар-кольцо даёт rcut-halo, а EAM нужно материализованное 2·rcut 3-зонное окно {j-1,j,j+1} (width≥2·rcut ⇒ окно полно). **E5b-1 (F4):** `eam_window_layout` — единый источник истины окна (CPU+GPU), устраняет center-vs-edge off-by-one. **E5b-2:** одно-узловой GPU EAM-ring ≡ serial `zone_eam_pass` ПОБИТОВО (gather → E5-ядра дословно → scatter → device-VV). **E5b-3a:** независимый оракул-гейт — GPU ≡ `eam_direct_fp64` (O(N²) FP64) <1e-10 + forward-only poison детектируем (зубы). **E5b-3b:** **стриминговое z>1 кольцо** — `EamRing` policy-темплейтнут по window-force (CPU-дефолт byte-identical; GPU-политика `cuda/eam_window_force_gpu.cuh` гоняет E5-ядра), `EamGpuRing` наследует ВСЮ проверенную оркестровку (jthread/node, second-forward-hop send-delay, FSM, Λ-chain, PBC defer_head) ⇒ bitwise ≡ CPU-ring по построению. `Test_CUDA_EAM_Ring` (10): GPU≡CPU bitwise (free+PBC, по-проходная PE/INV-9), 1-vs-z, anti-deadlock z=1..5, **независимые fp64-оракулы (free+PBC <1e-9 — ловят выпавшего донора, к чему 1-vs-z слеп)**, Q23.40 (fb=40). memcheck/racecheck чисто. **Транспорт host-orchestrated** (позиции на хосте, силы на GPU) — корректное кольцо. **Транспорт host-orchestrated** (позиции на хосте, силы на GPU). **E5c (первый кусок) ВЫПОЛНЕН (2026-06-17, состяз. дизайн+приёмка):** EAM cell-list культинг + бейкофф — `cuda/zone_eam_cells.cuh` (`eam_density/force_cells_kernel`: 27-cell loop из пар-cells, сетка над ВСЕМ окном dropped-donor-safe; **bitwise ≡ all-window** по B1). `Test_CUDA_EAM_Cells` (5): A cells≡all-window сырой int64 ПОБИТОВО (fb=44+40), B dropped-donor гейт vs `eam_direct_fp64` (free+steep+**PBC-seam**); memcheck/racecheck чисто. **Бейкофф:** cells сокрушают all-window **8.3×(N=864)→31×(4k)→117×(16k)**, atom-steps/s →2.06e7 (≡ флагман), grid-build пренебрежимо; realized ниже hit-rate-предсказания (27-cell over-fetch ×6.4). **TileMask-ВЕРДИКТ: NO-GO (отложить в ReaxFF)** — cells дают доминирующий выигрыш; residual = over-fetch (cell-size лёвер, НЕ mask-reuse); TileMask амортизирует перечисление по МНОГИМ проходам, EAM культит только 2 ⇒ маргинально (тот же вывод M4-B на LJ/Morse, теперь ИЗМЕРЕН на EAM; исходный вопрос пользователя закрыт). `Bench §E5c`. **Отложено (E5c+):** TileMask (до ReaxFF), PersistentVerlet-EAM, device-resident D2D транспорт (рычаг #2), MEAM/Tersoff/ReaxFF. (суб-rcut биннинг + cells-в-кольцо — ВЫПОЛНЕНЫ, см. ниже E5c-ring/E5c-subrcut.)

**M6 PHYSICS SUITE ВЫПОЛНЕН (2026-06-18, ветка `opus`; состязательный дизайн ДО + приёмка ПОСЛЕ — workflow `wf_3a0aa75f-4f3`; фальсифицируемые гейты заморожены в заголовках инструментов):** закрытие физ-критерия M6 (EAM/coexistence, не парные; one-phase нагрев запрещён — `VALIDATION_EXPERIMENT §5/§7`). **(a) NVE энерго/импульс-дрейф на наборе систем** (`tools/eam_drift.cu`, Al_zhou, N∈{864,2048,4000}, 20k шагов): **относит. сохранение энергии size-independent 1.1e-7** (FP64-пол round-off, НЕ VV-усечение: dt/2 не уменьшает в 4×, знак прыгает run-to-run, max|ΔE|≈секулярный ⇒ ограничено-симплектично; ~1000× ниже «excellent NVE» <1e-4 и ~1e5× ниже Морзе+shift ~1e-2 — гладкий setfl-cutoff); импульс 3–9e-11 (dt·√steps VV-округление, Σсил=0 ТОЧНО по int64-B1). Предрег. 1e-6 kT/(нс·dof) был мис-специфицирован → пере-калибровано к измеренному полу (×2, конвенция M3.5). **(b) статистич. RDF** (`tools/eam_rdf_stat.cu`+`Test_CUDA_EAM_RDF`): врем-усреднённый g(r) из NVE-траектории от LAMMPS-равновесной конфигурации, **косинус-перекрытие 0.99997** vs замороженный LAMMPS (E7 проверял пики 1 кадра; здесь вся кривая). **(c) двухфазный coexistence T_m** (`tools/eam_coexist.cu`+`reference_data/eam_al/coex_*`): LAMMPS строит s/l-плиту (8×8×24=6144, freeze-solid-half melt + NPH couple-xy P≈0, T плывёт к T_m) → **T_m^LAMMPS=642.3±1.6 K**; наш НЕЗАВИСИМЫЙ NVE-движок на той же конфигурации: **G1 |T_m^ours−LAMMPS|=5.7 K ≤25** (636.6 K), G1b reseed-аттрактор 2.9 K ≤10 (НЕ coast), G3 интерфейс Φ>0.5 bimodal каждый кадр, G4 энергия 2e-7, G5 импульс 1.7e-10, G6 ⟨P⟩=0.004 GPa — **все engine-vs-LAMMPS гейты PASS**. Заявление: «движок воспроизводит LAMMPS на этом setfl», НЕ булк-T_m Al, НЕ bitwise. **Находка (G2 non-blocking):** Al_zhou T_m≈642 K занижает эксп. Al (933 K) на −32% — свойство Zhou-2001 EAM-базы, не движка (предрег. полоса [930,1120] предполагала overshoot — фальсифицировано измерением). Методология/провенанс — `reference_data/eam_al/coex_README.md`.

**E5c-ring — переносится ли куллинг на ЖИВОЕ GPU-кольцо? ИЗМЕРЕНО (2026-06-18, ветка `opus`; состяз. дизайн `wf_701417ea-979` + приёмка `wf_03da14fd-3d8` ACCEPT-WITH-FIXES):** интеграция cells в живое `EamGpuRing` была сделана раньше (`4098ac0`, default ON, побитово); открытый вопрос — переносится ли изолированный куллинг-выигрыш на host-оркестрованное mutex-сериализованное кольцо. `tools/bench_eam_ring.cu` (+ `--free` у `bench_eam` для apples-R_kernel): **TRANSLATES** — нагруженное число R_ring=4.30 @ N=16384 (внутрихарнесное cull/all-window, z=1 whole-window free-z), η≈1.03 (±20% кросс-харнес). **Ring-internal per-phase таймер** (`#ifdef TDMD_EAM_RING_TIMERS`, байт-идентичный дефолт — `Test_CUDA_EAM_Ring` 11/11): **[h2d 1% | kernel 99% | d2h+sync 0%], f=0.99** ⇒ кольцо 99% **kernel-bound** (атрибуция дата-driven, не провизорная). Axis B (z=5): **SPEEDUP_z=0.99** — z>1 concurrency-dead (mutex на null-stream). **ДВА рычага:** (1) per-window over-fetch (~6.4×, R_kernel лишь ~4× при rcut=10.1) ⇒ **суб-rcut биннинг**; (2) z>1 ⇒ **снятие mutex / device-resident D2D** (реальный multi-zone payoff — multi-GPU M5b). Оговорки: 1.10e6 a-st/s — ВНУТРИКОЛЬЦЕВОЕ (free-z/z=1/fp64), НЕ флагман (~19× ниже M5a 2.06e7); cells регрессируют при N≲3000. Числа/провенанс — `Bench §E5c-ring`.

**E5c-subrcut — суб-rcut биннинг (per-window рычаг #1) РЕАЛИЗОВАН (2026-06-18, ветка `opus`; состяз. дизайн `wf_d4bdf675-970` + приёмка `wf_16a305ad-656` ACCEPT):** §E5c-ring показал кольцо 99% kernel-bound, ядро упёрто в 27-cell over-fetch (~6.4–7.6× при rcut=10.1). Ячейки ~rcut/k, стенсил ±s_d, **s_d=ceil((rcut+pad)/c) по РЕАЛИЗОВАННОМУ c (НЕ из k)** ⇒ покрывает ≥rcut ⇒ сырой int64 **побитово == all-window для ЛЮБОГО k** (B1); парный путь не тронут (default `cell_div=1`). Гейты (cells 32/32, ring 12/12): G0 (k=1 байт-идентично), A-k (cells(k)≡all-window k∈{2,3,4}), **B-k** (≡`eam_direct_fp64` оракул — независимый свидетель выпавшего донора), **P-k** (poison-зубы), W-k (n=2k+1 граница), C-k (живое AUTO+k кольцо, 1-vs-z); memcheck/racecheck чисто. **Бейкофф (N=16384):** k=3 оптимум — **≈1.71× live-ring** (1.088e6→1.860e6 atom-steps/s, reps=8), побитово-идентично; k=4 регрессирует (729-cell enum). **Default = AUTO (`cell_div=0`):** host-side эвристика в `ensure_grid_geometry` (резолв ОДИН раз, ноль device-работы) `k=clamp(round(cbrt((атомов/ячейку@k1)/2.5)),1,4)` ⇒ k=3 для Al_zhou, **k=1 для коротко-rcut/разрежённых (без регрессии)**. Производство получает выигрыш адаптивно, без магической константы. Оговорки: 1.71× — z=1/этот setfl; z>1-cull выводится (kernel-bound), не таймится. Числа — `Bench §E5c-subrcut`.

**E5c-concurrency — рычаг #2 (device-resident / per-node-stream): ПРОБА GO, живой NULL, ОТКАТАН (2026-06-18, ветка `opus`; состяз. дизайн `wf_d75b4a72-21c`).** §E5c-ring: z>1 concurrency-dead (`SPEEDUP_z=0.99`, mutex на null-stream). Measure-first: **ncu pre-check** (бесплатно) — ядра latency-bound (SM 38%, FP64 24% абс., occupancy 7%, DRAM ~1%), НЕ saturated ⇒ headroom есть (опровергает аналитический «85% SM-fill» дизайна). **Проба** (`tools/probe_eam_concurrency.cu`): N независимых EAM-цепочек на N stream'ах vs serial → **SPEEDUP_conc 1.49× (2 streams) / 1.71× (3)** ⇒ GO на уровне ядер. **Построил #1** (per-node scratch+streams, снял mutex; детерминизм цел — `Test_CUDA_EAM_Ring` 12/12 побитово), но живой **Axis-B `SPEEDUP_z`=1.00 (NULL)** — потолок пробы съеден host-оркестрацией (пер-проходный gather+transport+sync) + сериализацией пайплайна. **Откатан** (нулевой single-GPU выигрыш; пер-узловые stream'ы — не то, что нужно M5b). #2/#3 (full device-resident, positions-on-device) — отложены в M5b (их ценность — cross-GPU edge). Числа/провенанс — `Bench §E5c-concurrency`.

**Состязательный анализ «переносить ли отложенные перф-рычаги в общий слой?» + DESCRIPTOR FIREWALL ВЫПОЛНЕН (2026-06-18, ветка `opus`; анализ `wf_d083d3e0-669`, приёмка PR `wf_c5f5a82c-de5`).** Вопрос: для будущих MEAM/Tersoff/ReaxFF/ML — сделать TileMask/concurrency/sub-rcut дефолтными в общем слое? **Вердикт: в основном НЕТ (measure-first per-potential остаётся главным).** TileMask→DEFER-AND-GATE (MEAM/ReaxFF-PR, MB1–MB4; `Mask_triplet` — MEAM-угловая физика, нечего строить без ядра), device-resident→DEFER (M5b; single-GPU NULL, **усиливает-NULL** для тяжёлых ядер), verlet-default→DEFER (memory-gated). **ОДНО исключение — correctness-firewall, не оптимизация:** GPU-аккумулятор предполагает симметрию Newton-3 `q(j)=−q(i)` (EAM ок), а MEAM/Tersoff угловые члены НЕсимметричны (`needs_transpose`, сила в третий атом k). Без гейта будущий MEAM-PR молча гонит симметричный аккумулятор → **детерминированно-но-физически-неверные** силы, невидимые всем consistency-гейтам. **PR (`35cf18b`):** `GpuEamWindowForce::assert_supported(passes)` бросает на needs_transpose/iterative/wrong-kind; `EamRing::run()` зовёт через `if constexpr requires` (CPU-оракул — compile-time no-op); зубы на ВСЕХ 3 слотах + seam-is-live static_assert; пар-ядро расфоркнуто `±1→±s_d` (байт-идентично). **Честный scope (приёмка MF-A):** гейт только на EamRing-пути; `eam_gpu_run_singlenode` (z=1 драйвер физ-suite) НЕ закрыт — помечен `FIREWALL GAP`, закрыть при MEAM (MB1) + промотировать контракт политики до C++20-concept (MB1/MB2). EAM байт-идентичен (13/13 ring + 32/32 cells + 7/7 zones + CPU 22/22 + memcheck 0). Дальше — на выбор: MEAM/Tersoff (расширить физику; гейты MB1/MB2 + transpose-аккумулятор строить ВМЕСТЕ с ядром), M5b (multi-GPU, при ≥2 GPU), или другой трек.

**УГЛОВАЯ ФИЗИКА НАЧАТА — SW-лестница (Stillinger–Weber). PR-1 (T1) ВЫПОЛНЕН (2026-06-18, ветка `opus`, `b339f69`; состяз. дизайн `wf_5294df4d-65c` + приёмка `wf_9f9ea78c-d7b` ACCEPT-WITH-FIXES — все применены).** Первый ANGULAR-потенциал — несимметричная сила в третий атом k (`needs_transpose`, чего симметричный EAM-аккумулятор `q(j)=−q(i)` не умеет). **SW первым, НЕ Tersoff/MEAM** — решающее: **независимость оракула** (all-triplets оракул SW не делит derivative-алгебру с проверяемым путём, только перечисление; Tersoff-оракул сам дифференцирует b_ij → баг зеркалится в оба, зубы слепнут; MEAM-screening делает кандидатный набор third-atom-зависимым). Арка SW→Tersoff→MEAM. **CPU-only** (zone/ring/GPU — T3/T3b/T5; LAMMPS-golden — T7): `potentials/sw.hpp` (φ₂+φ₃, `sw_triplet` 3-атомная сила, `sw_direct_fp64` FP64-оракул scatter-per-center + POISON, `sw_run_fixed` int64 TRANSPOSE-REPLAY gather-per-owner = drop-in WinForce для T3), `gen/diamond_si.hpp` (общая фикстура: идеальная решётка вакуумна — Δ=0 в тетраэдре ⇒ возмущённый diamond Si), `effective_range_for("sw")={2,true}` (ДОКАЗАНО ≡ EAM: крыло достаёт 2·rcut). **Два пойманных бага:** (1) **знак угловой силы** (+2λεΔ через ∂cosθ/∂x_j = −(1/r_ij)(…), POSITION-градиент; дизайн-док имел −2; поймал ТОЛЬКО независимый FD — оракул делит алгебру и слеп); (2) **детерминизм (MUST-FIX приёмки, INV-9)** — j/k-слоты `sw_triplet` равны в ℝ но НЕ побитово (FP-неассоциативность ~1 ULP straddl-ит Q24.40 tie) ⇒ `sw_run_fixed` НЕдетерминирован под relabeling, маскирован tie-free фикстурой; фикс — **wing-canonical** (`gg=g_ij·g_ik` 2-операндный коммутатив + зеркальный порядок радиальных операндов ⇒ k-слот = побитовый образ j-слота), physics-neutral (Δ≤1.78e-15). Зубы: `TripletWingSymmetryIsBitwise` (100k триплетов, ДЕТЕРМИНИРОВАННО падает на triplet 0 без фикса; relabeling-ансамбль — флаки-зубы). **Приёмка Test_SW 6/6 + suite 23/23 (G-NOOP):** G-FD<1e-5, G-TI/MB1<1e-12, G-B1, G-ORACLE/MB2 (gather≡scatter<1e-9 + count + POISON>1e-3), G-VACUITY; **ВНЕШНЕ LAMMPS `pair_style sw run 0` (64-ат. diamond Si): PE −252.9945896 vs −252.99459 MATCH, max|ΔF|=4.96e-6** (форма+параметры валидированы, де-рискует лестницу). **T3 обязан загейтить:** wing-canonical `sw_triplet` → дословно в T5 GPU; PBC-seam триплеты (ДВА крыла независимо wrap — нужен PBC FP64-оракул); wing-gather достаёт 2·rcut (throw при ширине зоны <2·rcut); ГЕОМЕТРИЧЕСКИЙ poison-класс (index-straddle POISON слеп к Δ≈0 near-tetrahedral). `tools/sw_lammps_check.cpp` — one-off harness (T7-precursor, не в CMake).

**SW T3 — serial zone-SW ВЫПОЛНЕН (2026-06-18, ветка `opus`, `882b56b`; состяз. дизайн `wf_c90b13cf-202` + приёмка `wf_e015c591-703` ACCEPT-WITH-FIXES).** Угловая сила на 3-зонной TD-резиденции {S_{j-1},S_j,S_{j+1}} (width≥2·rcut), serial. Зеркалит EAM-драйвер (`zone_eam_window` ПЕРЕИСПОЛЬЗОВАН дословно). **T2 — уже покрыт PR-1** (G-B1 relabeling + wing-symmetry зубы + count==oracle). `potentials/sw_zone.hpp`: `sw_window_force` (per-OWNED transpose-replay над собранным окном; (i) φ₂ сила+энергия по НИЖНЕМУ ГЛОБАЛЬНОМУ key; (ii) φ₃ CENTER → f_i+E3+n_triplets ОДИН раз; (iii) φ₃ WING → только f_j; ALL-WINDOW nbr-списки для owned И halo); `sw_zone_pass` (gather/scatter + residence THROW + min-image guard: каждое периодич. ребро >2·rcut — у SW ДВА крыла независимо wrap). **Три доказательства (приёмка верифицировала свипом 960 конфигов, worst count-delta=0, |F−oracle|=1.4e-9):** 1-vs-z побитово (окно держит каждый триплет owned-атома; halo-nbr полнота у края окна держится — цепочка o→i→m достаёт ровно 2·rcut, открытый cutoff r2<rc2 спасает границу); PE побитово (энергия триплета — зоной центра ОДИН раз; φ₂ — нижним глоб. key; members партиция); two-wing PBC (окно копирует глоб. координаты by-value в общий PairGeom ⇒ reduce-входы байт-идентичны монолиту; 373 seam-триплета, 74 с ОБОИМИ крыльями wrap). **Фиксы приёмки:** Gap-B — добавлен `EXPECT_NEAR(rz.pe, ro.pe, 1e-6)` (якорь PE к НЕЗАВИСИМОМУ оракулу, не только к sibling sw_run_fixed); Gap-A — исправлены ложные комментарии (φ₂ глоб-key БЕЗ зубов на serial-пути: `zone_eam_window` сортирует окно ⇒ local≡global; key DEFENSIVE для T3b с НЕсортированным ring-окном). Test_SW_Zone 9/9 + suite 24/24. **T3b обязан загейтить:** `SwWinForce::assert_supported` ПРИНИМАЕТ needs_transpose=true (НЕ копировать `GpuEamWindowForce`, который на нём бросает — φ₃ transpose-replay ЕСТЬ правильный механизм; отвергать только iterative/wrong-kind); сигнатура (нет rho_cap, +n_triplets); ring-vs-serial PE побитово + ring-vs-oracle. **[Этот «глоб-key load-bearing в T3b» claim T3b ИЗМЕРИЛ ЛОЖНЫМ — см. ниже.]**

**SW T3b — threaded SwRing ВЫПОЛНЕН (2026-06-19, ветка `opus`, `3c8aacc`; состяз. дизайн `wf_4752ee3c-cde` + приёмка `wf_58a3f69f-bd0` ACCEPT-WITH-FIXES).** SW на проверенном TD-кольце. **РЕШЕНИЕ — ФОРК, не рефактор:** `EamRing<Real,Math,WinForce>` хардкодит EamPotential+`.math`+`zone_eam_pass` ⇒ `EamRing<Real,SwParams,…>` не компилится; traits-shim переписал бы тип-член/ctor проверенного оракула. Пустой `git diff` форк делает EAM-no-op доказательство ПРОВЕРЯЕМЫМ ОСМОТРОМ (сильнее codegen-эквивалентности) + дом-стиль (EamRing сам форкнут из TimeConveyor). `sw_ring.hpp` = `eam_ring.hpp` character-for-character + РОВНО 5 свопов (pot-тип, `rcut_=pot.sw.rcut()` метод, `fb_`/density удалён, `rho_cap=0.0` ignored, t0 `sw_zone_pass`). EAM ring/zone/cuda-firewall git-НЕТРОНУТЫ (14/14+10/10). `SwWinForce<Real>` stateless (static_assert trivially_copyable — shared по z-jthread), compute() 14-арг const drop-in; `assert_supported` ИНВЕРТИРУЕТ `GpuEamWindowForce` — ПРИНИМАЕТ needs_transpose (φ₃ transpose-replay ЕСТЬ механизм), отвергает count/non-Force/iterative/symmetric-φ₃-trap (закрывает FIREWALL GAP на SW-шве; EAM-гейт НЕ ослаблен). +ctor min-image guard (кольцо обходит guard `sw_zone_pass`). **ИЗМЕРЕНИЕ-ПЕРВЫМ ПЕРЕВЕРНУЛО ДИЗАЙН (Gap-A T3):** дизайн+T3-приёмка утверждали «φ₂ глоб-key становится load-bearing в T3b» (ring-окно несортировано, ~29% пар флипнут под local ⇒ wrong PE). **ИЗМЕРЕНО ЛОЖНЫМ:** local-key poison даёт PE ПОБИТОВО-ИДЕНТИЧНУЮ (|delta|=0). φ₂-пара охватывает только СОСЕДНИЕ зоны (rcut<width); `eam_window_layout` эмитит [pred][center][succ] ⇒ соседние зоны в ОБОИХ окнах в одном относит. порядке (cyclic-predecessor-first, вкл. PBC-шов) ⇒ local-index считает каждую пару РОВНО раз = глоб-key. Флипы меняют КАКАЯ зона атрибутирует энергию, не СКОЛЬКО раз; φ₃-энергия center-owned (не key-gated); силы per-owner. Глоб-key — DEFENSIVE (робастен к reorder), оставлен; ложные комментарии исправлены (COMMENT-ONLY, код байт-идентичен). Продакшн юзает глоб-key ⇒ правота/неправота находки не шлёт баг. **Гейты Test_SW_Ring 6/6 + suite 25/25:** G1 ring(z=1)≡serial-VV побитово; G2 1-vs-z free-z{2,3,5}+PBC{2,3,6} fixed+auto-dt; G3 траектория within 1e-6 от serial-VV на НЕЗАВИСИМОМ `sw_direct_fp64` (MB2-свидетель, к чему 1-vs-z слеп); G-PE ring-PE==serial==oracle + poison-PE побитово==correct (находка); G6 anti-deadlock z=1..6; G7 инверт-firewall. **T5 (GPU SW) обязан:** φ₃ transpose-аккумулятор (сила в третий атом k, q(k)≠−q(i) — симметричный EAM-int64 не умеет; firewall форсит постройку); `eam_window_layout` как единый источник block-order для GPU-gather; независимый FP64-оракул (G3) как GPU MB2-гейт; two-wing min-image guard на device.

**SW T4 — NVE energy/momentum conservation ВЫПОЛНЕН (2026-06-19, ветка `opus`, `795dad1`; приёмка `wf_6048347f-bb6` ACCEPT-WITH-FIXES).** Физ-валидация кольца T3b как NVE-интегратора, БЕЗ нового кода. Методология M6 (калибровать потолок к ИЗМЕРЕННОМУ полу, не предрегистрировать). **ИЗМЕРЕНО (diamond-Si, 300K, 1фс, 3000 шагов):** ЭНЕРГИЯ rel_exc=1.63e-6 (отлично, ~60× ниже лит. <1e-4) — dt-halving ratio 3.37 ⇒ ~94% VV-усечение (dt²) + ~6% round-off, НЕ force-shift-leak (SW exp-envelope гладко зануляет value+derivative; max|dE| run-length-ИНВАРИАНТ до 30 пс ⇒ ограниченная осцилляция, секулярный slope — fit-артефакт). **МОМЕНТУМ — НАХОДКА: dp=1.49e-8 (НЕНУЛЕВОЙ), vs EAM int64-ТОЧНЫЙ ~1e-11.** EAM-симметрия `q(j)=−q(i)` сокращается в int64 (rint нечётный); SW-триплет квантует f_i,f_j,f_k НЕЗАВИСИМО (transpose-replay), rint НЕ аддитивен ⇒ `rint(f_i)+rint(f_j)+rint(f_k)≠0` хотя f_i=−(f_j+f_k) в double (G-TI<1e-12); ~25% триплетов несут O(1)-квант residual ⇒ Σf≠0 ⇒ моментум random-walk-ит ∝√steps. Физ-следствие B1, не баг. Потолки rel_exc<5e-6 (×3.1), dp<1e-7 (×6.7). **MUST-FIX приёмки (применён):** дискриминатор `MomentumFloorIsInt64Quantization` ДОКАЗЫВАЕТ находку в CI — FP64-оракул (`sw_direct_fp64`) держит Σf round-off (<1e-12), int64 (`sw_run_fixed`) дрейфит на кванте (≠0, <1e-9); force-asymmetry баг сломал бы ОБА ⇒ зазор исключает баг + даёт dp<1e-7 зубы. Test_SW_NVE 3/3 + suite 26/26. **T5/T7:** GPU-SW реплицирует этот int64 momentum-floor (дискриминатор = CPU↔GPU bitwise-оракул несимметричной записи; GPU-симметричный shortcut молча сломал бы — needs_transpose firewall = страж); T7 наследует smooth-cutoff энерго-baseline; dp∝√steps — известное ограниченное свойство для длинных NVE.

**SW T5 — GPU Stillinger–Weber ВЫПОЛНЕН (2026-06-19, ветка `opus`, `aeb0182`; состяз. дизайн `wf_f13d5221-b7e` + приёмка `wf_e5012682-84e` ACCEPT, no MUST-FIX).** Самая крупная ступень: угловая сила на GPU — φ₃ TRANSPOSE-аккумулятор (сила в третий атом k, q(k)≠−q(i)), чего симметричный EAM-GPU-int64 не умеет; needs_transpose firewall форсит постройку. `cuda/zone_sw.cuh` `sw_force_kernel`: ОДИН поток на OWNED-атом o, весь transpose-replay в int64-РЕГИСТРЫ, write-once (без cross-atom записи и force-atomics); (i) φ₂; (ii) φ₃ CENTER по парам соседей o (f_i+E3+count); (iii) φ₃ WING — для каждого соседа i brute-force re-scan окна за соседями m, f_o += f_j из {i;o,m} (o в j-слоте — несимметричная запись). Per-thread локальный буфер kMaxNbr=64 (у SW-Si ~4 соседа в rcut — 2-я оболочка 3.84Å ВНЕ rcut=3.77; 4× запас); overflow ⇒ guarded HALT (бросок ДО writeback). `sw.hpp`: sw_phi2/sw_g/sw_triplet → `TDMD_HOST_DEVICE` (single-source; CPU-SW 4/4 байт-неизменны). `cuda/sw_window_force_gpu.cuh` `GpuSwWinForce` (14-арг compute() drop-in, инверт-firewall ПРИНИМАЕТ needs_transpose); `GpuSwRing = SwRing<double,GpuSwWinForce<double>>` через T3b policy-ctor — НОЛЬ изменений оркестровки. **КОНТРАКТ (честный, ОБРАТНЫЙ EAM-spline):** CPU↔GPU — ТОЛЕРАНТНОСТЬ (~1e-9 сила/~1e-6 PE; exp/pow расходятся libm-vs-CUDA ~1 ulp, ситуация Морзе), НЕ bitwise. GPU-ВНУТРЕННЯЯ детерминированность (run-to-run, 1-vs-z) — bitwise ИСКЛЮЧИТЕЛЬНО по B1 int64-order-freedom (wing-canonical на GPU inherited/defensive — каждый owned в СВОЁМ j-слоте, нет swapped-slot коллизии — НЕ load-bearing как на CPU). Независимый FP64-оракул (`sw_direct_fp64` scatter-per-center, иное перечисление) <1e-9 — единственный MB2-свидетель. **Гейты test_cuda_sw 7/7 + test_cuda_sw_ring 5/5 (GPU-suite 12/12, CPU-SW 4/4):** G2 CPU↔GPU толерантность; G3 GPU-bitwise (run-to-run+window-permutation raw int64); G4⭐ FP64-оракул free+PBC; G5⭐ poison-зубы (pert=0.20 закреплён); G6⭐ momentum-дискриминатор (GPU int64 Σf∈(0,1e-9) vs FP64 round-off — находка T4 реплицируется на device); G7 overflow-HALT; G8 ring 1-vs-z bitwise; G10⭐ FP64-траектория; G11 firewall; G12 anti-deadlock. memcheck+racecheck+initcheck ЧИСТО. **T5b(cells)/T7:** независимый FP64-оракул — единственный MB2-свидетель (dropped wing-triplet слеп к 1-vs-z); cells обязаны остаться bitwise к all-window по B1; uninitialized-D2H (унаследован от EAM) починить на общем шве ДО MEAM.

**SW T7 — physics-suite closure (LAMMPS golden + RDF) ВЫПОЛНЕН (2026-06-19, ветка `opus`, `f8d2597`; приёмка `wf_87bf7121-a1b` ACCEPT-WITH-FIXES). ЛЕСТНИЦА SW ЗАВЕРШЕНА.** Внешняя валидация против ЗАМОРОЖЕННЫХ LAMMPS-эталонов (CI без LAMMPS), БЕЗ нового кода — закрывает SW-suite (точность через силы, структура через RDF; зеркалит EAM E2+E7/M6). `reference_data/sw_si/`: **силы** `sw_si_64.data/.forces` (LAMMPS `pair_style sw run 0`; `Test_SW_LAMMPS` гоняет `sw_direct_fp64`, PE<1e-6 + max|ΔF|<1e-9; ИЗМЕРЕНО 1.03e-12 — exp/pow-ulp-пол; зубы: λ-ошибка 1e-5 валит; dPE≠0 ⇒ эталон независим от нашего кода; int64-путь `sw_run_fixed` тоже совпадает ~7e-12 ⇒ zone/ring/GPU транзитивно LAMMPS-валидны); **RDF** `sw_rdf_216.data/.gr` (600K-равновесный 216-ат. + LAMMPS врем-усреднённый g(r); `Test_SW_RDF` гоняет наш NVE + cosine-overlap **0.999815**, nn-пик 6.53 vs 6.64; зубы: scrambled→0.29, без φ₃→0.66); **`gen_forces.in`/`gen_rdf.in`** — закоммиченные LAMMPS-скрипты регенерации (MUST-FIX приёмки — у RDF не было provenance; теперь воспроизводимо по EAM-прецеденту). **ЛЕСТНИЦА SW: T1(CPU-физика)→T3(serial-zone)→T3b(threaded-ring)→T4(NVE+momentum-находка)→T5(GPU φ₃-transpose)→T7(LAMMPS-golden+RDF) — SW end-to-end, CPU+GPU, внешне валидирован.** (T5b cells-culling отложен measure-first; melt-Tm вне scope, [ENG].) **ПЕРЕНОС НА TERSOFF (ключевая эскалация):** у SW all-triplets оракул делит с проверяемым путём только ПЕРЕЧИСЛЕНИЕ (FD/oracle независимы от derivative-алгебры); у Tersoff оракул делит b_ij bond-order алгебру ⇒ баг знака/алгебры зеркалится в ОБА, self-checks слепнут ⇒ **внешний LAMMPS-golden становится ЕДИНСТВЕННЫМ независимым свидетелем derivative-алгебры Tersoff** (из nice-check в load-bearing-гейт; бюджетировать + 2-я геометрия фикстуры под bond-order угловую зависимость).

**TERSOFF Te1 — CPU analytic bond-order ВЫПОЛНЕН (2026-06-19, ветка `opus`, `c2ecabd`; состяз. дизайн `wf_1816b773-9f2` + приёмка `wf_78840416-8cf` ACCEPT-WITH-FIXES — все применены).** Первый bond-order потенциал — угол ВНУТРИ `b_ij(ζ_ij)` (2-проходная directed-bond структура, НЕ явный 3-body как SW); самый error-prone код проекта. Арка SW→Tersoff→MEAM, [ENG]. **ЭСКАЛАЦИЯ (load-bearing):** у SW all-triplets оракул делит только ПЕРЕЧИСЛЕНИЕ ⇒ FD-of-energy независим; у Tersoff force-recompute оракул делит ВСЮ b_ij chain-rule алгебру ⇒ слеп к знак/алгебра-багу. ⇒ **FD-of-ENERGY** (числ. −dU/dx, юзает только value-хелперы, НИКОГДА `*_d`) + **внешний LAMMPS-golden** — ЕДИНСТВЕННЫЕ независимые свидетели алгебры; all-bonds оракул ПОНИЖЕН до enumeration/MB2. `tersoff.hpp`: `TersoffParams` (Si.tersoff verbatim + c1..c4 + assert m∈{1,3},D≤R), HOST_DEVICE хелперы РЕПЛИЦИРОВАНЫ ПОСИМВОЛЬНО из `pair_tersoff.cpp` (ters_fc/fc_d, fa/fa_d [fA сворачивает fc], gijk/gijk_d, ex_delr [±69.0776 clamp], 5-веточный малый-ζ bij/bij_d [фикс 9/30/2015], zetaterm_d [сила в третий атом], costheta_d); `tersoff_energy` (двучленный LAMMPS-split: repulsive Σ_{i<j} fc·fR БЕЗ ½ + attractive ½ Σ_directed fc·b_ij·fA, center-i owns), `tersoff_direct_fp64` (FP64-оракул 2-pass scatter + drop_class POISON), `tersoff_run_fixed` (int64 Q24.40 scatter — G-B1 свидетель; transpose-replay → Te3). Дескриптор [BondOrder(transpose=false, **FP64 ζ** — order-free single-window; FixedAccum ζ сломал бы FD-of-energy), Force(transpose=true)], {2,true}. **ЗНАКОВАЯ ЛОВУШКА (класс −2→+2):** two-body delx=x_i−x_j (проект); three-body delr1=x_j−x_i (=−dx) для хетов+radial, **fpair=fforce·(1/r)** — баг, который ПОЙМАЛ FD (force_zeta НЕ сворачивает /r, только repulsive). **ВАЛИДАЦИЯ:** G-FD=4.16e-08 (поймал /r-баг); LAMMPS-golden силы **4.26e-14**, PE Δ≈1e-13 vs замор. `pair_style tersoff run 0` (64-ат. diamond Si; energy==LAMMPS 1e-13 ⇒ force=−dU/dx ⇒ нет компенсирующего бага). `reference_data/tersoff_si/` (data/forces/gen_forces.in/README, CI без LAMMPS). **MUST-FIX приёмки (ИЗМЕРЕННЫЙ молчаливый зазор):** `ters_fc_d` (производная косинус-cutoff) СТРУКТУРНО не трогается на станд. фикстуре (все связи r∈[1.95,2.75], taper [2.8,3.2] ⇒ max|fc_d|=0); sign-flip баг в ней был НЕВИДИМ всем 7 гейтам. Фикс: G-FD теперь ещё на РАСТЯНУТОЙ a0=6.70 фикстуре (nn≈2.90 в taper) + прямой `CutoffDerivativeMatchesFD`. SHOULD-применены: `MomentumFloorIsInt64Quantization` (int64-пол SW-T4 реплицируется — fp64 Σf 1e-14 vs int64 8e-12, следствие B1); inert BondOrder accum_fracbits аннотирован (ζ FP64, Te3 не должен alloc FixedAccum). Гейты Test_Tersoff 8/8 + Test_Tersoff_LAMMPS 1/1 (suite 30/30). **Te3/Te5 обязаны:** transpose-replay (gather-per-owner — zetaterm f_k пишет в halo чужой зоны, запрещён как scatter на кольце); FD-of-energy+LAMMPS остаются единств. свидетелями алгебры; Te5 GPU 2-pass CPU↔GPU bitwise vs FP64-оракул + хранит G-LAMMPS; momentum-floor реплицируется на GPU (симметричный shortcut молча занулит — needs_transpose firewall форсит transpose-аккумулятор); taper-shell фикстура остаётся; multi-element golden для λ₃ vs λ₂ (для Si замаскировано, отложено).

**TERSOFF Te3 — serial zone-Tersoff (4-role transpose-replay) ВЫПОЛНЕН (2026-06-19, ветка `opus`, `90a72a9`; состяз. дизайн `wf_fc46259f-f57` + приёмка `wf_bdae88f3-f59` ACCEPT-WITH-FIXES — применён).** Bond-order угловая сила на 3-зонной TD-резиденции {S_{j-1},S_j,S_{j+1}} (width≥2·rcut), serial. Зеркалит SW T3 (`sw_zone.hpp`) + ПЕРЕИСПОЛЬЗУЕТ `zone_eam_window` дословно. **НОВАЯ МАШИНЕРИЯ — TRANSPOSE-REPLAY:** Te1 `tersoff_run_fixed` — SCATTER (центр i пишет f_i,f_j,f_k в три атома); на кольце zetaterm f_k пишет в halo чужой зоны — ЗАПРЕЩЕНО ⇒ каждый owned o СОБИРАЕТ только свою силу. В ОТЛИЧИЕ от SW (wing-canonical схлопнул крыло в ОДНУ роль), у Tersoff directed bond b_ij≠b_ji БЕЗ j/k-симметрии ⇒ ЧЕТЫРЕ роли получателя: (1) PAIR/repulsive (симметричная сила o, энергия lower-global-key); (2) CENTER-i (ζ_{o,j} по соседям o, attractive-radial f_i + angular f_i, attractive-энергия + n_triplets owned ЗДЕСЬ один раз); (3) ENDPOINT-j (bond (i,o), ζ_{i,o} по соседям I, radial f_j + angular f_j, без энергии); (4) THIRD-k (bond (i,j), ζ_{i,j}, angular f_k для triplet (i,j,o)). `tersoff_zone.hpp`: `tersoff_window_force` (4-role gather, единый mandatory `zeta_replay` helper — никакая роль не переупорядочивает nbr[c]) + `tersoff_zone_pass` (gather/scatter + two-bond min-image THROW + residence THROW + forward-only POISON). **ПОБИТОВО к `tersoff_run_fixed` ДОКАЗАНО (§3 биекция — приёмка верифицировала мультимножество: angular 17340≡17340, radial 3352≡3352, repulsive 1676≡1676, only_scat=0, only_gath=0; Role-4 не вакуумна — 9.4% триплетов):** z=1≡run_fixed dF=0, 1-vs-z dF=0, z=6 vs независимый оракул dF=6e-12, counts 768 bonds/4608 triplets == oracle. **MUST-FIX приёмки (measure-first честность, паттерн SW T3b — коммент+тест, БЕЗ изменения поведения):** дизайн/G5 ЗАВЫСИЛИ ζ-replay order как «load-bearing — иначе другая int64-сила». ИЗМЕРЕНО ЛОЖНЫМ на Te3-фикстурах: полностью РЕВЕРСНУТОЕ окно ПОБИТОВО == run_fixed при perturb ≤ 0.70 (ζ-order delta ~1e-11 НИЖЕ Q24.40 кванта после b_ij-масштабирования), флипает ровно один квант (9.1e-13) только при perturb ≥ 0.80. ⇒ global-id сортировка DEFENSIVE/robustness, НЕ load-bearing на фикстурах (та же находка, что у SW T3b global key). Header+G5 исправлены; G5 теперь дискриминатор (реверс-окно, оба режима). Сортировка+helper оставлены защитно (Te3b/Te5 carry-forward). Гейты Test_Tersoff_Zone 11/11 (G0-G8 + G-PE-независимый-оракул + G-PBC-seam + G-MOMENTUM; G3 — И n_bonds И n_triplets). Suite 31/31. **Te3b обязан:** форк `eam_ring.hpp`/`sw_ring.hpp` → `TersoffRing`; `TersoffWinForce` policy + firewall ИНВЕРСИЯ (accept needs_transpose; accept [BondOrder,Force] НЕ SW's [Force,Force]); second-forward-hop; ζ-replay single-helper инвариант.

**TERSOFF Te3b→Te7 ВЫПОЛНЕНЫ автономным прогоном (2026-06-19, ветка `opus`; каждый — состяз. дизайн ДО + приёмка ПОСЛЕ; ЛЕСТНИЦА TERSOFF ЗАВЕРШЕНА — end-to-end CPU+GPU, внешне валидирована).** **Te3b — threaded TersoffRing** (`c98c505`; дизайн `wf_0dd9c6c5-129` + приёмка `wf_87426386-c25` ACCEPT): ФОРК `sw_ring.hpp` (S1-S5 свопы + policy-блок; EAM/SW-кольца + Te1/Te3 git-БАЙТ-НЕТРОНУТЫ, F-NOOP). `TersoffWinForce` firewall ИНВЕРСИЯ — accept `[BondOrder,Force]` (НЕ SW `[Force,Force]` — копия SwWinForce ложно отвергла бы pass0==BondOrder; приёмка верифицировала инверсию в ОБЕ стороны). **ГЕНУИННАЯ НОВИЗНА — CANONICAL ζ-SORT:** кольцо собирает окно НЕСОРТИРОВАННЫМ (block order), serial `tersoff_zone_pass` сортирует (`zone_eam_window`); FP64 ζ-sum order-sensitive ⇒ `TersoffWinForce::compute` сортирует окно по global key (perm+reorder+remap owned+scatter-back, приёмка верифицировала: 0 owned-mismatch + bitwise PE на реверс-окне) ⇒ кольцо ПОБИТОВО к serial (G1; транзитивно к run_fixed/FD/LAMMPS). SW не нуждался (int64-order-free). Test_Tersoff_Ring 6/6. **Te4 — NVE** (`74dd464`; приёмка single-reviewer ACCEPT): rel_exc=8.13e-7 (VV-truncation, dt-halving 3.98; C1-косинус-cutoff НЕ течёт при 300K — нет атомов в taper [2.8,3.2]), dp=4.70e-9 (int64 non-symmetric momentum floor, как SW T4; кросс-валид. 6.5e-11·√3000≈3.6e-9). Test_Tersoff_NVE 3/3. **Te5 — GPU** (`d711092`; дизайн `wf_d662cfdd-eca` + приёмка `wf_2ae2e50b-f2a` ACCEPT-WITH-FIXES): `cuda/zone_tersoff.cuh` `tersoff_force_kernel` (ОДИН поток/owned, 4-role transpose-replay в int64-РЕГИСТРЫ, write-once, kMaxNbr=64+HALT; helpers УЖЕ HOST_DEVICE из Te1 ⇒ tersoff.hpp нетронут); `GpuTersoffWinForce` (host canonical-sort + memset-initcheck + `[BondOrder,Force]` firewall). КОНТРАКТ: CPU↔GPU ТОЛЕРАНТНОСТЬ (exp/pow/sin ulp), GPU-внутр. bitwise по B1. **GPU canonical-sort DEFENSIVE** (приёмка ИЗМЕРИЛА: GPU 1-vs-z bitwise БЕЗ сортировки — block-окно z-независимо — + single eval sub-quantum; over-claim в баннере исправлен). Приёмка ВПРЫСНУЛА dropped-Role-4 баг → G2/G4/G6 поймали (max|ΔF|≈1.34) ⇒ FP64-оракул (scatter-per-center, иное перечисление) ловит выпавшую роль. **G7-находка:** Tersoff repulsive A·exp(−λ₁r) ОГРАНИЧЕН (~10577 эВ/Å) ⇒ близкая пара НЕ переполняет B1 (в отличие от SW r⁻¹²) — HALT даёт kMaxNbr-cap. test_cuda_tersoff 8/8 + test_cuda_tersoff_ring 5/5, memcheck+initcheck+racecheck ЧИСТО. **Te7 — physics closure (RDF)** (`3fd7dc0`; приёмка single-reviewer ACCEPT): forces-golden уже в Te1; Te7 = врем-усреднённый g(r) нашего NVE vs замор. LAMMPS — cosine overlap **0.999829** (nn-пик 6.31 vs 6.47; приёмка: melted→0.28, shift→0.92, всё <0.99 ⇒ гейт дискриминирует). `reference_data/tersoff_si/tersoff_rdf_216.data/.gr/gen_rdf.in`. Test_Tersoff_RDF 1/1. **ЛЕСТНИЦА: Te1(CPU+LAMMPS-golden)→Te3(serial-zone)→Te3b(ring+canonical-ζ-sort)→Te4(NVE+momentum-floor)→Te5(GPU φ₃-transpose)→Te7(RDF).** Арка SW→Tersoff→MEAM: SW и Tersoff завершены; MEAM (screened angular) — следующая эскалация, transpose-аккумулятор + firewall на месте.

**MEAM Me1 — single-element Si MEAM ENERGY ВЫПОЛНЕН (2026-06-19, ветка `opus`, `8a730ff`; состяз. дизайн `wf_63aa980a-eab` + приёмка `wf_187342d3-fb8` ACCEPT-WITH-FIXES — применены).** Самый сложный потенциал проекта: SCREENING S_ij=Π_k S(C_ijk) делает candidate set ЗАВИСИМЫМ ОТ ТРЕТЬЕГО АТОМА (k между i,j удаляет связь i-j — «Mask_triplet», который firewall предвосхищал) + угловые парциальные плотности ρ⁰..ρ³ + фон ρ̄=ρ⁰·G(Γ) + embedding F(ρ̄). Арка SW→Tersoff→MEAM, [ENG], single-element Si. **SCOPE = ENERGY-FIRST** (аналит. сила ~470 строк → Me2: сила дифференцируется вручную через screening+4 плотности+embedding+G(Γ), ~3× Tersoff, а Te1 ВСЁ РАВНО привёз sign-баг, который поймал только FD ⇒ запереть value-алгебру на golden ДО производной). `meam.hpp` (~580 строк, крупнейший хедер): `MeamParams` (Si-параметры хардкод из library.meam + recompute() выводит re, t1_eff=2.82 [augt1], ebound, rho_ref, φ-сплайн; throw-стражи на КАЖДОЙ отложенной ветке — nn2/ialloy/mix_ref_t/bkgd_dyn/ibar≠1/emb_lin_neg/erose_form, НЕ молча); HOST_DEVICE leaf-хелперы; `meam_getscreen` (value-path screening, эллипс C_ijk + Cmin/Cmax/fcut, colinear-non-rejection trap); `meam_calc_rho1`; `meam_dens_final` (v2D/v3D Voigt, Γ, G=exp(Γ/2)); φ через эталонную структуру (phi_meam/densref/compute_reference_density/cubic-spline/ZBL-blend); `meam_energy` (+poison flag = S≡1 дискриминатор); `meam_run_fixed` (int64 Q24.40 плотности + итог в EnergyAccum = G-B1). Дескриптор [Density, Embedding, Force(needs_transpose)] + инверт-firewall (screening 3rd-atom write — легитимная несимметричная запись, нового PassDecl-поля не надо). effective_range_for("meam")={2,true}. **ВАЛИДАЦИЯ:** total PE −243.388597227171 vs LAMMPS −243.388597227171 (Δ~3e-13 — приёмка верифицировала ГЕНУИННОСТЬ, не компенсирующий баг: два структурно разных перечисления совпадают, все константы выведены с нуля, poison расходится 17.9 эВ; std::exp совпал с LAMMPS fm_exp, гейт 1e-4 для кросс-геометрии). **LAMMPS-golden — ЕДИНСТВЕННЫЙ свидетель value-алгебры** (энергия MEAM гоняет screening+density алгебру ⇒ self-check оракул делит её, слеп — эскалация из Tersoff-«nice-check» в load-bearing). **MUST-FIX приёмки (рецидив находки Te1 fc_d):** ЧАСТИЧНАЯ полоса screening (0<S<1) — буквальная сигнатура MEAM — СТРУКТУРНО МЕРТВА на diamond-фикстуре (310 S=0, 0 partial; каждый perturb, который её заселил бы, ВАЛИТ ZBL-страж; reviewer mutation-доказал: порча fcut(C) там ничего не меняет). Фикс: `meam_tri3.data/.energy` — свободный 3-атомный кластер (i-j заслонён k, S≈0.50, все связи выше r_zbl) со своим LAMMPS-golden (PE=−0.935624255785264); `PartialScreeningClusterMatchesLammps` независимо валидирует частичное умножение. G-B1 фикс: итог энергии в int64 EnergyAccum (плотности были order-free, FP64-итог дрейфил 2e-14 при relabel). SHOULD-применены (branch-стражи, README-ZBL-disclosure, G-VACUITY→kill-band). Гейты Test_MEAM 6/6 + Test_MEAM_LAMMPS 2/2 (total+cluster). Suite 36/36. EAM/SW/Tersoff байт-нетронуты. **ЛЕСТНИЦА MEAM ЗАВЕРШЕНА (2026-06-20, ветка `opus`; каждая ступень — состяз. дизайн ДО + приёмка ПОСЛЕ для крупных; end-to-end CPU+GPU, внешне LAMMPS-валидирована — силы И структура).** Самый сложный потенциал проекта целиком: **Me2 — CPU аналит. СИЛА** (`2bb2ee3`; приёмка `wf_91e10a54` нашла РЕАЛЬНЫЙ `dCfunc2` sign-баг + STALE-БИНАРНИК, маскировавший красный cluster-гейт ⇒ урок: ВСЕГДА чистый ребилд + load-bearing свидетель ДО заявления green): screening 3rd-atom ∂S/∂x_k transpose-replay; FD-of-energy (force-алгебра-свидетель) + LAMMPS-golden диамант/`meam_tri3`. **Me3 — serial zone** (`ca87f2f`; приёмка `wf_2b81f7f4` ACCEPT): transpose-replay (реплей Me2-scatter по каждой directed-связи с резидентным центром, scatter ТОЛЬКО в owned ⇒ побитово к Me2 ПО ПОСТРОЕНИЮ); reach {2,true} ТОЧНО 2·rc (k∈nbr[i]<rc, НЕ √ebound·rc; 200-trial sweep ноль побегов); diamond БИНАРНО-S (screening-сила МЕРТВА) ⇒ единств. фикстура `partial_slab` (8 partial-S триплетов; FP64-оракул `meam_direct_fp64` — ЕДИНСТВ. свидетель выпавшего screening-k). **Me3b — threaded MeamRing** (`2abf620`): 4-й форк кольца (EAM→SW→Tersoff→MEAM); two-PE int64-fold; canonical ζ-sort LOAD-BEARING (screening-product Π_k order-sensitive); firewall [Density,Embedding,Force(needs_transpose)] ИНВЕРСИЯ (accept screening-write, reject EAM-симметричную ловушку). **Me4 — NVE** (`35473d4`): энергия rel_exc 4.1e-6 (dt² VV-truncation; гладкий fcut не течёт); momentum-floor SCREENING-СПЕЦИФИЧЕН — int64 Σf≠0 на slab (transpose), ТОЧНО 0 на diamond (binary-S симметрия). **Me5 — GPU** (`1d03f2a`; дизайн `wf_d39146d0` + приёмка `wf_54d1ee49` ACCEPT-WITH-FIXES): САМОЕ КРУПНОЕ — 3 ядра (density→embedding→force, `cuda/zone_meam.cuh`), 27 int64 density-lanes, CPU F-NOOP рефактор ПЕРВЫМ (общие `meam_*_device` хелперы, 39/39 байт-идентично); CPU↔GPU ТОЛЕРАНТНОСТЬ, GPU-внутр. bitwise (B1, --fmad=false); FP64-оракул на partial-фикстурах — ЕДИНСТВ. dropped-k свидетель; **MUST-FIX = 4-й рецидив structurally-dead trap**: `−coef2` radial-taper sign silent на всех 3.2Å фикстурах (rnorm=8⇒dfcut=0⇒coef2=0) ⇒ фикс taper-band триплет (bond 3.95Å, rnorm=0.5, 0<S<1) + FD-of-energy (зубы flip→1.47 эВ/Å); **ВОССТАНОВЛЕНИЕ:** случайный `git checkout meam.hpp` стёр uncommitted Me5-правки → восстановлены из scratch-копий ревьюеров + верифицированы чистым 39/39+GPU. **Me7 — RDF** (`716f60f`): врем-усреднённый g(r) NVE vs замор. LAMMPS — cosine overlap **0.99993** (nn-пик 7.97 vs 8.02). ЛАДДЕР: Me1→Me2→Me3→Me3b→Me4→Me5→Me7. **Отложено measure-first:** denser multi-k partial-фикстура, multi-element. Арка SW→Tersoff→MEAM ЗАВЕРШЕНА. **Me5b — CELLS-CULLING MEAM-GPU + ПЕРВЫЙ angular флагман 10⁶ ВЫПОЛНЕН (2026-06-21, ветка `opus`; дизайн `docs/_meta/M5b_MEAM_CELLS_DESIGN_2026-06-21.md`; полная летопись — `Bench §Me5b`).** Новый хедер `cuda/zone_meam_cells.cuh` (`zone_meam.cuh`/`zone_cells.cuh`/`meam.hpp` + EAM/SW/Tersoff БАЙТ-НЕТРОНУТЫ = F-NOOP; CPU-suite 40/40): K1→`meam_density_cells_kernel`, K3→`meam_force_cells_kernel` (4 суб-cull: o-nbr-build центр o + Role A кэш nb[] + Role B/C re-center grid на i), K2 (embedding) verbatim. **CRUX — canonical-k cull:** screening `sij=Π_k fcut` (FP-произведение) + `dscrfcn=Σ_k` (FP-сумма) НЕ под B1, а all-window УЖЕ sorted-key; cell-walk видит k в scatter-порядке ⇒ FP реассоциируется ⇒ cells-aware helper СОБИРАЕТ стенсильных кандидатов-k → СОРТИРУЕТ по global key → product/dscrfcn в том порядке ⇒ побитово == sorted all-window для ЛЮБОГО scatter (штатное недетермин. `cell_scatter_kernel` переиспользовано). Reach суб-cell (√ebound·rc≈1.04rc<2rc) ⇒ ОДИН rc-padded grid. Интеграция: `cull`/`cell_div` AUTO в `GpuMeamWinForce`/`GpuMeamWindowState` (зеркало EAM E5c; firewall нетронут; AUTO k=clamp(round(cbrt(атомов/ячейку/2.5)),1,4) — для MEAM-Si даёт **k=1**). **Гейты `Test_CUDA_MEAM_Cells` 11/11 (memcheck+racecheck+initcheck чисто; приёмка `wf_614bb673` ACCEPT-WITH-FIXES — M1+M2 применены):** G-A (cells≡all-window СЫРОЙ int64 ∀cell_div∈{1,2,3,4} — diamond+slab free+PBC+taper — canonical-k forcing), **G-B** (cells≡`meam_direct_fp64` оракул на PARTIAL с `n_partial>0` THROUGH cull — единств. dropped-screening-k свидетель; diamond binary-S Role-C МЁРТВ ⇒ НЕ свидетель), G-POISON (стенсиль-too-small + drop_class — оба расходятся), G-W (tight-PBC k=4), G-kMaxNbr (16<64), **G-OVERFLOW** (приёмка M1: canonical-k буфер `kkey/krik2/krjk2[64]` теперь bound-checked + sticky-HALT как `nb[]` — 80-ат. dense-cube ОБЯЗАН HALT, не молчаливый OOB; раньше cull ввёл silent-OOB регрессию — all-window helper стримит без буфера), G-momentum (slab Σf∈(0,1e-9), diamond exact). **НАХОДКА (G-SORT, measure-first):** дизайн+Me3b предсказали canonical-k sort LOAD-BEARING; ИЗМЕРЕНО на diamond (16 screening-k/связь)+slab — all-window САМ order-invariant (skip_sort `|delta|=0`, реассоциация суб-квант Q24.40) ⇒ **DEFENSIVE, НЕ load-bearing** (тот же вердикт Te3b/SW-T3b/Te5); cull оставлен — делает cells побитово ПО ПОСТРОЕНИЮ + робастен к денсер фикстуре. **Бейкофф:** R_cull растёт КРУТО (7.6×@1k→36.9×@4k — вложенный screening убирает ЛИШНИЙ O(m)-фактор сверх EAM); all-window MEAM intractable >16k (K3@1e6=15.3с/запуск). **ФЛАГМАН: 6.04e4 atom-steps/s @ 10⁶ — ПЕРВОЕ angular flagship-scale число** (изолир. ядро z=1/free-z/fp64; НЕ живое кольцо, НЕ bitwise-EAM); память **1.46 GiB @ 10⁶** (дизайн оценивал 6–8 GiB — ×4–5 ниже; 10⁷ влезает, gate к 10⁶). **МЕТОД-ОГОВОРКА (приёмка M2):** clocks НЕ залочены (нет root) ⇒ число шумное; 6.04e4 ВОСПРОИЗВОДИМО STANDALONE (повтор 6.06e4 @ 10⁶), но ~2× проседает под сопернич. за GPU (приёмка с 3 || ревьюерами видела 3.16e4 — contention-артефакт, мерить в одиночку). **ЧЕСТНО:** 6e4 на ~100× НИЖЕ дизайн-прогноза 3e6–7e6 — K3 register/stack-bound (**cells K3 = 254 рег, 5008 Б** по cuobjdump; all-window K3 = 255/3248, разница +1760 Б = canonical-k буферы; occ ~8%, <1 волна ниже ~21k — НЕ spill, явный local-state), вложенный Role-C cull тяжелее «3–4× EAM»; пропускная растёт с N (2.6e3→6.04e4 @ 1k→1M, насыщение волнами). **Рычаги (отложены measure-first, по отдаче):** (1) СТРУКТУРНЫЙ — собрать in-rc список i ОДИН раз, переиспользовать по Role-C (~8×, побитово-эквив. ибо G-SORT defensive; снижает И буферы ⇒ И register-pressure — связаны); (2) K3 register-restruct; (3) AUTO-k тюнинг (k N-чувствителен/шумен — в части прогонов +30–40% на средних N, точную «+%» не фиксируем под незалоченными clocks); (4) denser multi-k фикстура; (5) cells-в-живое-MeamRing. **ФЛАГМАН УГЛОВ ЗАВЕРШЁН — SW-T5b + Tersoff-Te5b cells (2026-06-21, ветка `opus`; приёмка `wf_3d184ec4` ACCEPT, без MUST-FIX; летопись `Bench §Angular flagship`).** Cell-list культинг для SW (`zone_sw_cells.cuh`) + Tersoff (`zone_tersoff_cells.cuh`) — форки Me5b (all-window ядра + `zone_cells.cuh` + sibling-cells БАЙТ-НЕТРОНУТЫ = F-NOOP; CPU 40/40). **РАЗЛИЧИЕ:** SW **int64-order-free** ⇒ cells побитово к all-window НАПРЯМУЮ по B1 (БЕЗ canonical sort — G-A без сортировки = доказательство, EAM-like); Tersoff bond-order ζ=Σ_k FP-сумма ⇒ **canonical-ζ-cull** (gather→key-sort→sum, MEAM-like; G-SORT DEFENSIVE). **M1-страж (урок Me5b silent-OOB) ВЕРИФИЦИРОВАН ИНЪЕКЦИЕЙ:** каждый gather-буфер bound-checked+sticky-HALT; ревьюер удалял КАЖДЫЙ → sanitizer ловил OOB (оба форка). Гейты `Test_CUDA_SW_Cells` 9/9 + `Test_CUDA_Tersoff_Cells` 10/10 (G-A ∀cell_div + G-B FP64-оракул non-vacuous + G-POISON + G-OVERFLOW + momentum; sanitizers чисто). **ФЛАГМАН-ТАБЛИЦА @ 10⁶ (изолир. ядро z=1/free-z/fp64, воспроизв. на idle-GPU): SW 2.60e6 [FMA 2.82e6] · Tersoff 9.15e5 [FMA 9.89e5] · MEAM 1.77e5 [FMA 1.77e5] — ПЕРЕМЕРЕНО idle 2026-06-21.** **REGISTER-LEVER ИЗМЕРЕН И ОПРОВЕРГНУТ (2026-06-21, `Bench §Register-lever`):** гипотеза «register-bound, рычаг ~8× (gather-i-once)» ЛОЖНА — ncu: все три **FP64-TRANSCENDENTAL-bound** (`sm__pipe_fp64` SW 65%/Tersoff 60%/MEAM 60%, память 1-3%). `__launch_bounds__` (рег 253→128, 2→4 блока) NULL; gather-i-once НЕ помог бы (память не узкое место); FMA (единств. дешёвый рычаг на FP64-bound) +8% SW/Tersoff, **0% MEAM** (трансценденты exp/pow/log насыщают). **ПОПРАВКА: записанный MEAM 6.04e4 был contention-артефактом — истинный idle 1.77e5 (×2.9)**; «обратно ∝ register-pressure» — мис-атрибуция (рег — ПРОКСИ FP64-работы). Cull РАБОТАЕТ (R_cull ∝N: SW 12×, Tersoff 88×). Большие рычаги (отложены, риск): алгоритм. сокращение трансцендентов / mixed-FP32 (ломает bitwise); MEAM нужен алгоритм. рычаг (FMA 0%). Суб-rcut k ортогонален (AUTO=k=1 @ 10⁶). **LIVE-RING CELLS DEFAULT-ON (2026-06-21):** `Gpu{Meam,Sw,Tersoff}WinForce` ctor `cull` флипнут false→true (гейты зелёные, AUTO cell_div, как EAM E5c). Новый гейт `LiveRingCellsBitwiseToAllWindow` (×3): cells-кольцо ≡ all-window-кольцо ПОБИТОВО в живом кольце (z=1 + 1-vs-z, free+PBC) ⇒ cull переживает host-оркестрацию. R_ring: SW 2.3× / Tersoff 12.6× / **MEAM 17.5×** (all-window MEAM/Tersoff-кольцо ИНТРАКТАБЕЛЬНО на масштабе — 153с/6144ат ⇒ cells ВКЛЮЧАЮЩЕЕ условие, не оптимизация). z>1 на 1 GPU concurrency-dead (multi-zone выигрыш — M5b). CUDA 57/57, CPU 40/40. **M7 — UX/DevOps (CI/контейнеры/dashboard) ВЫПОЛНЕН (2026-06-21, ветка `opus`; дизайн `_meta/M7_DEVOPS_DESIGN_2026-06-21.md`; приёмка single-reviewer ACCEPT).** (а) **Полный CI** `.github/workflows/ci.yml` — 4 джобы: build-test (g++/clang++, ccache, `ctest -LE cuda` — критерий M7, зелёный БЕЗ LAMMPS/GPU); lint (`-Werror` сборка core+CLI БЛОКИРУЮЩАЯ + clang-format ADVISORY `continue-on-error` — дерево намеренно НЕ нормализовано repo-wide); cuda-compile (в `nvidia/cuda:13.1.0-devel-ubuntu24.04`, mpi{OFF,ON}, **arch 75 НЕ 70** — CUDA 13.1 выкинул sm_70, COMPILE-ONLY без ctest); gpu (self-hosted INERT — `if: workflow_dispatch||push-to-main`, НЕ pull_request: public-repo security gate). (б) **Контейнеры** `deploy/` — apptainer/tdmd.def + docker/Containerfile (base CUDA-13.1-devel, %test = CPU smoke `ctest -LE cuda`, без GPU на build) + spack/spack.yaml (toolchain; yaml-cpp/gtest остаются FetchContent) + README (NOW-vs-LATER). (в) **Live ANSI dashboard** — ⭐ хук `ConveyorOptions::on_pass` (`std::function<void(long,const PassStats&)>`, fire ПОСЛЕ B1-редукции на conveyor.hpp:551, empty-default = байт-no-op) ОБСЕРВАЦИОННЫЙ; `cli/dashboard.hpp`+`src/cli/dashboard.cpp` (pure `render_plain` + ANSI \033[H/SGR + sparkline, surface: progress/E/NVE-drift/dt/T/vmax/**Rbuf-headroom** [TD-distinctive]/HALT) + `cli/progress_monitor.hpp` (on_pass→snapshot под одним lock, рендер OFF-thread ~10Hz) + TTY-детект (isatty&&TERM&&!CI&&!NO_COLOR→ANSI на stderr; иначе plain log) + `--dashboard` флаг. **⭐ A7 — ГЕЙТ ДЕТЕРМИНИЗМА** (`Test_Conveyor.A7DashboardHookIsBitwiseObservational`): нетривиальный on_pass (checksum по всем PassStats-скалярам, concurrent по z-узлам) ⇒ финальное состояние ПОБИТОВО == detached run (1-vs-z + NVE) — load-bearing доказательство, что хук невидим для INV-9 (приёмка: хук СТРУКТУРНО инертен — const PassStats, stats write-only post-fire, запись в atoms_ затирается scatter_final). Гейты Test_Dashboard 7/7 + A7. Отложено-LATER (честно): multi-node MPI/spack install (M5b), in-flight mid-ring rescue (t0-state на HALT), async-I/O (отдельный sub-PR — не запутывать determinism-хук), repo-wide clang-format нормализация (отдельный style-PR — 144-file churn реверчен). Числа INTERNAL (НЕ живое кольцо, НЕ EAM-сравнение; под GPU-сопернич. ~2-3× ниже). cuda→tdcu alias в 4 sibling-тестах (CUB-clash). ✅ Пост-M5a (2026-06-12): **INV-7-пул + потоковая P₁** — закон ёмкости Z·S ≥ n+слак (найден встречным дедлоком; ≡ дисс. «≥6 зон/процессор»), S = min(n+2, max(8, ⌈n/Z⌉+4)); память 10⁷: z=4/n=16 4.36→2.15 GiB, z=4/n=32 → 1.63 GiB (×2.6); перф нетронут. ✅ Пост-M5a (2026-06-13): **FMA-сборка** (`bench_conveyor_fma`: +4–7%, 1M mixed 2.50e7 a-st/s; `--verify-det`: GPU-детерминизм при FMA ПОБИТОВО — эмпирика под B1) + **NVTX** (`TDMD_WITH_NVTX`; nsys-диагноз: end+sync 32.6% — пер-зонная D2H+sync END-скаляров = следующая цель оптимизации). ✅ Пост-M5a (2026-06-13): **батчинг END-sync** — один sync/проход вместо n (стражи реплеем перед последней отправкой; лемма: проваленный проход не шлёт последнюю зону ⇒ префикс stats цел; побитово прозрачно); end+sync 32.6→12.1%, NVE 50k −10%; новый лидер flush = межстримовый idle (рычаг M5b). Локальные рычаги исчерпаны. См. `Roadmap` M5b/M6. Переход к следующей вехе — только после зелёного критерия текущей; вехи с внешним триггером (M5b) — отложенные/параллельные и не блокируют (`Roadmap`, правило ворот). Сборка: `cmake -S . -B build -G Ninja && cmake --build build && ctest --test-dir build` (45 CPU-теста: +M6 `Test_ManyBody_Config`/`Test_EAM`/`Test_EAM_Spline`/`Test_EAM_Zone`/`Test_EAM_Ring`/`Test_EAM_RDF`; +SW `Test_SW`/`Test_SW_Zone`/`Test_SW_Ring`/`Test_SW_NVE`/`Test_SW_LAMMPS`/`Test_SW_RDF`; +Tersoff `Test_Tersoff`/`Test_Tersoff_LAMMPS`/`Test_Tersoff_Zone`/`Test_Tersoff_Ring`/`Test_Tersoff_NVE`/`Test_Tersoff_RDF`; +MEAM `Test_MEAM`/`Test_MEAM_LAMMPS`/`Test_MEAM_Zone`/`Test_MEAM_Ring`/`Test_MEAM_NVE`/`Test_MEAM_RDF`; +M4-N `Test_Metrics`/`Test_Physical_Oracle`; +M7 `Test_Dashboard`; +PR-0a `Test_W_Contract`; +PR-1 `Test_EAM_Donation`; +PR-0b `Test_Skin_Budget`; +PR-0c `Test_Rebuild_Epoch`; CUDA +`Test_CUDA_EAM`/`Test_CUDA_EAM_Ring`/`Test_CUDA_EAM_Cells`/`Test_CUDA_EAM_RDF`; +SW-GPU `Test_CUDA_SW`/`Test_CUDA_SW_Ring`/`Test_CUDA_SW_Cells`; +Tersoff-GPU `Test_CUDA_Tersoff`/`Test_CUDA_Tersoff_Ring`/`Test_CUDA_Tersoff_Cells`; +MEAM-GPU `Test_CUDA_MEAM`/`Test_CUDA_MEAM_Ring`/`Test_CUDA_MEAM_Cells`; M6-physics инструменты `eam_drift`/`eam_rdf_stat`/`eam_coexist` + Me5b `bench_meam_cells` собираются с `-DTDMD_WITH_CUDA=ON`); CUDA-вариант: `cmake -S . -B build-cuda -G Ninja -DTDMD_WITH_CUDA=ON && cmake --build build-cuda && ./scripts/gpu_gate.sh build-cuda`. Python-тулинг (numpy для `tools/gen_fcc.py`): системного pip нет — `python3 -m venv .venv && .venv/bin/pip install numpy` (каталог `.venv` в .gitignore).

**АУДИТ W-ФАЗЫ/СОСЕДЕЙ + ГИГИЕНА + PR-0a W-КОНТРАКТА ВЫПОЛНЕНЫ (2026-07-02, ветка `opus`).** (1) **Аудит** `docs/_meta/AUDIT_W_PHASE_NEIGHBORS_2026-07-02.md` (облачный, 2 воркфлоу ~2.5M токенов) влит (`26630ce`): диагноз — w-фаза существует как FSM-состояние, но не как работа (в 4 many-body кольцах ДЕМОНТИРОВАНА: SPHERE+START подряд; цена = ×3-избыточность density/nbr/gather в kernel-времени); верифицированный план — **W-контракт** (донационные int64-батчи при CoRes-событиях, обобщение дисс. w-scatter на любой B1-пасс) + ZoneNeighborStore; честные ставки: EAM density 3×→1× (гейт R_W≥1.15 с откатом), SW/Tersoff v1 — ноль (transcendental-bound), главная ценность — M5b/MPI+ReaxFF; PR-лестница 0a..7. (2) **Гигиена §7** (`382e32e`): контракт схема↔код (ключи трёх сортов: действующий/[INERT] с `[config] note`/[FATAL-в-CLI]; enum `integrator` валидируется; зубы DocumentedSections/InertKeys/BadIntegrator); решение «**CLI v1 = парное демо**» записано (ConfigSchema §5, точное EAM-сообщение); README M2.5→M7 (флагманы с оговорками isolated-kernel/idle, CPU↔GPU bitwise только LJ@fmad=false), Roadmap-шапка+M6/M7 DONE, Skeleton §1 as-built; фантом `-DWITH_LAMMPS` снят везде; верификация 4-линзовым воркфлоу (15 находок применены). (3) **PR-0a — W-контракт, шаг 0** (дизайн `wf_ef54881d` 11 агентов + приёмка `wf_d3ebb5ae` 4 линзы: mutations/fnoop ACCEPT, spec/vacuity ACCEPT-WITH-FIXES — все применены; дизайн-док `_meta/PR0A_WCONTRACT_DESIGN_2026-07-02.md`): чисто-аддитивный F-NOOP — `PassDecl` += хвостовые `w_class`/`donor_roles` (инертные дефолты; позиционные инициализаторы целы), `validate_pass_decls` (kAccumB1⇒fb>0, запрещён на BondOrder [ζ-класс], roles-only-on-kAccumB1, reserved-бит, iterative; ЧЕСТНО: canon-dependent-addend класс машинно НЕ ловится — только состяз. дизайн+FP64-оракул), **`donation_layout(j,n,pbc)`** (единый источник расписания донаций рядом с `eam_window_layout`; шов pbc(n−1,0) эмитится на j=n−1 СТРУКТУРНО — до finalize(n−1); 8 SPEC-клауз в doc-comment; потребители — PR-1/2), want_closure_mask+closure_bit (леджер = existing `contrib_mask`), **concept `WindowForcePolicy`** — class-scope static_assert во всех 4 кольцах УБИВАЕТ opt-in `if constexpr requires` (молчаливый обход теперь compile-error; `CpuEamWindowForce`+2 LocalKey получили делегирующие assert_supported), **FIREWALL GAP `eam_gpu_run_singlenode` ЗАКРЫТ** (хвостовой `passes`-параметр=`kEamPassDecls`+мандаторный `assert_eam_symmetric_passes` первой строкой; 8 колл-сайтов не тронуты; GPU-политика делегирует тому же гейту — одна истина), CI-слепота закрыта — `tools/cuda_compile_check{,_tersoff}.cu` (member-scoped `run()` explicit instantiation — `template class` статически ill-formed [convenience-ctor], MUST-FIX верификации; сплит из-за clash `kMaxNbr` sw↔tersoff; облачная джоба компилирует угловые GPU-политики впервые, обе ноги mpi проверены локально). **Гейты:** ЧИСТЫЙ ребилд → CPU 42/42 (+`Test_W_Contract` 18: T1-T10/T-SIM/T-EAM/T-CPT с kill-мутациями) + CUDA 17/17 таргетов (161.6с — нулевой налог) + T-GAP (4 poison-класса THROUGH драйвер) + **A/B-hex пояс: HEAD≡PR байт-в-байт** (eam_drift 500 шагов) + 5/5 kill-мутаций приёмки убили свои зубы (M1 шов→3 красных, M4→compile-time). **Попутно:** фильтр `gpu_gate.sh` отстал от M4-S — `*OffEquilibrium*` (3000-шаг стресс, ИЗМЕРЕНО 2ч+ под racecheck при 20-с коротких Verlet тех же код-путей) добавлен в racecheck-исключения (memcheck-покрытие сохранено, прецедент Replica36). **Отложено (D1-D8):** MPI compile-TU, хуки on_zone_arrival/on_edge в концепте (войдут с потребителями PR-1/2), ρ_a≥0 setfl-страж, WContract-док+FSM-аддендум (PR-1), флипы w_class дескрипторов, canon-suffix, membership_ok мутационный зуб, вакуумный батч исполнительно. Дальше: PR-0b (skin_budget) / PR-1 (serial-оракул EAM-донаций) по лестнице аудита.

**PR-1 W-КОНТРАКТА — SERIAL DONATION ORACLE для EAM ВЫПОЛНЕН (2026-07-03, ветка `opus`; состяз. дизайн `wf_57f16ac5` [4 читателя → 2 дизайнера → судья → 4 верификатора, все ACCEPT-WITH-FIXES — вшиты] + приёмка `wf_9de494f1`; дизайн-док `_meta/PR1_DONATION_ORACLE_DESIGN_2026-07-02.md`).** Первый СОДЕРЖАТЕЛЬНЫЙ шаг W-трека: ρ собирается донационными int64-батчами по `donation_layout` (self intra-зонные пары + cross граничные, каждая пара РОВНО раз — вместо пооконного ×3-пересчёта) в персистентные пер-зонные аккумуляторы, ключёванные по zone_id; force/PE/min_r2/φ-once — C-фаза ДОСЛОВНО. **ПОБИТОВО ≡ `zone_eam_pass` на домене G-A** (силы всех атомов + PE + min_r2 + owned-ρ; halo-ρ ЛЕГИТИМНО расходится — full-ρ memcmp ложно-красный и запрещён как гейт). `eam_donation.hpp`: `assert_eam_donation_descriptor` (свободная функция — Т-13 seam, MUST-FIX верификации: EamPotential final), экзекьюторы (лемма W-1: негация/один-v-в-оба-конца), `check_donation_residence` (зеркало membership_ok, g=0.5(w−2rc), тест-only `g_override`), `eam_window_force_from_rho` (копия pass-2/3 с 3 дельтами: ρ-вход / cap ТОЛЬКО на owned-полной ρ в finalize [К3, trigger-set экв. держится на D3] / Trace), пошаговый `EamDonationPass` (леджер exactly-once THROW + вакуумные биты пустых зон + дедуп слотов pbc n=1 [К7 — находка слияния] + первый runtime-потребитель `want_closure_mask`), канонический `zone_eam_pass_donated` (defer_head-зеркало, fb-диспатч 44/40 от РАНТАЙМОВОГО density_fracbits). **§8.1 РЕШЁН: ARRIVAL-вариант** (M5b — донации как полезная работа при ожидании; формулировка-наследие PR-2 — `TD_MD_Core_WContract_v1_0.md` §9 [НОВЫЙ док, §1-§12] + [ENG]-аддендум §12 к ZoneFSM [append-only, fsm.hpp нетронут]). **D5-флип**: EAM Density → kAccumB1+self|lo|hi (первый шипнутый W-дескриптор; замки T-INERT/T9 ретаргетированы + T9-EAM). **D3 САДИТСЯ (измерено чисто)**: стражи ρ_a≥0 — ТОЧНЫЙ минимум кубиков сплайна (межузловой overshoot; узловая проверка НЕДОСТАТОЧНА — зуб Т-15б) + замкнутая форма аналитики (β>0∧rho_amp>0); Al_zhou min +2.9e-13, from_analytic ровно-0 принят (строгий <0). **ГЛАВНАЯ НАХОДКА ВЕРИФИКАЦИИ ДИЗАЙНА (measure-first переворот, 7-й случай):** деривация слияния «на полосе дрейфа (g_eam,g_pair] обе стороны одинаково слепы ⇒ G-A зелёный» ИЗМЕРЕНА ЛОЖНОЙ — упущен КЛАСС-3 (уже-донированное вне-оконное ребро читаемого halo, открывается ровно при D>g_eam): донированный персистент СТРОЖЕ-ПОЛНЕЕ окна ⇒ **G-A на полосе КРАСНЫЙ** (|ΔF|=1.24 эВ/Å), D7b-зуб реинстейтирован, g_eam несущий для САМОЙ эквивалентности. Гейты `Test_EAM_Donation` 15/15 (Т-1 G-A free n∈{1..8}+owned-ρ vs O(N²)-int64; Т-2 PBC+дедуп+seam-счётчик; Т-3 G-EDGE биекция vs brute + exactly-once THROW; Т-4 FP64-оракул <1e-10 [непереуступаемый intra-batch свидетель]; Т-5/6/7/8 POISON drop-batch/late-seam/skip-pair-ledger-blind/one-sided; Т-9 G-ORDER [исполнимый W-1]; Т-10 rho_cap trigger-set экв. + owner-атрибуция; Т-11 G-LCLOSE+D7 пять измерений [3-атомная ручная цепочка]; Т-12 fb40 β=3.3; Т-13 rollback-страж; Т-14 вакуум; Т-15 D3-зубы) + `Test_W_Contract` 19/19. Чистые ребилды: CPU 43/43, CUDA plain 17/17. Кольца/fsm/GPU-код git-байт-нетронуты; `eam_zone.hpp` — comment-only (§8.1 РЕШЕНО). **Дальше:** PR-2 (донации в живом EamRing — обязательства WContract §12) / PR-0b (skin_budget) / PR-3a-b (GPU, гейт R_W≥1.15).

**PR-0b W-КОНТРАКТА — ИЗВЛЕЧЕНИЕ `skin_budget.hpp` ВЫПОЛНЕНО (2026-07-03, ветка `opus`; приёмка `wf_4b71b7a4` 4 линзы: byte-identity/fnoop-scope/gpu-fnoop ACCEPT, teeth ACCEPT-WITH-FIXES — MUST-FIX применён).** Чистый code-motion (аудит §3.5): host-метод `GpuTimeConveyor::decide_pass` (Λ-скин-рекуррентность NL-INV-2a + K-aware fallback-гистерезис) извлечён БАЙТ-ИДЕНТИЧНО в новый чистый хедер `core/skin_budget.hpp` (`SkinParams{skin,K_on,K_off,hybrid}` + свободная `decide_pass`); метод стал тонкой config-binding-делегацией (call-sites 1123/1206 не тронуты, +1 include). Цель — ЕДИНЫЙ источник для порта соседей many-body-колец (PR-4) вместо пере-деривации + извлечённая логика впервые CPU-юнит-тестируема (в облаке без GPU). Новый `Test_Skin_Budget` (8 зубов: charge=2·R_buf, гистерезис K_on≥/K_off< sticky в обе стороны, turn-on→rebuild, fallback-idle, консерв-аккумулятор+reset+inclusive-граница, hybrid берёт tighter min-bound через no-hybrid-контроль, guard charge==0→+inf-не-NaN). **F-NOOP-гейт (байт-дрейф завалил бы любой):** verlet-тесты 5/5 ПОБИТОВО (`VerletHybridBitwiseAndLargerK` rebuild-счётчик совпал, `VerletHybridOffEquilibriumStress1vsZ` 1-vs-z 3000 шагов, reuse/fallback-vs-cells), MPI np2/np4 2/2 побитово, CPU-суйт 44/44. **MUST-FIX приёмки (5-рецидивный класс structurally-dead):** зуб `ZeroRbufInfiniteKTurnsOn` был ВАКУУМЕН — при skin=1.0 снятие стража даёт 1.0/0.0=+inf (не NaN), тест зелёный; страж load-bearing ТОЛЬКО при skin==0 (0/0=NaN) ⇒ переспецифицирован на degenerate skin=0 + добавлена inclusive-граница rebuild. Изменённые файлы: `conveyor_gpu.cuh` (include+враппер) + CMakeLists; `core/skin_budget.hpp`/`test_skin_budget.cpp` — новые. Дальше: PR-2 (донации в живом EamRing) / PR-3a-b (GPU, гейт R_W≥1.15).

**PR-0c W-КОНТРАКТА — КАРКАС СОСЕДЕЙ + `rebuild_epoch` ВЫПОЛНЕН (2026-07-03, ветка `opus`; состяз. дизайн `wf_f23caa75` [2 читателя→дизайнер→2 верификатора: wire-fnoop ACCEPT, structurally-dead ACCEPT-WITH-FIXES] + приёмка `wf_a9550b46`; дизайн-док `_meta/PR0C_ZONE_NBR_STORE_DESIGN_2026-07-03.md`).** Чисто-аддитивный F-NOOP. **ГЛАВНОЕ РЕШЕНИЕ ДИЗАЙНА (решающий фактор — урок structurally-dead PR-0a D2):** device-каркас `cuda/zone_nbr_store.cuh` (Tier-0/1/2 буферы+ядра+пер-ранговый local_epoch[]) ОТЛОЖЕН к своим потребителям (PR-1/PR-3a/PR-4); сейчас — ровно инертное поле + чистый примитив + device-FREE контрактный дом. (1) `ZoneHeader.rebuild_epoch` (uint32, инертно, ADD-alongside rebuild_now) сел в СУЩЕСТВУЮЩУЮ padding-дыру ⇒ **sizeof НЕ изменился (104→104, offset 68, d_full на 72)**, заперто machine-checked `static_assert(sizeof==104)`+`offsetof(d_full)==72` (SHOULD верификации: F-NOOP как гейт, не проза); wire-safe по построению (ZoneHeader едет в GpuHeader через memcpy(sizeof), обе стороны один компайл); grep — нигде не читается. (2) `core/rebuild_epoch.hpp` (NEW pure host, зеркало skin_budget) — `advance(prev,rebuild)` (монотонный счётчик, СТРОГО растёт ⟺ rebuild — L-SUP-мост) + `stale(local,hdr)`=`local<hdr` (ленивая MPI-материализация: equal reuse/lagging rebuild/ahead total); SSoT для PR-4. (3) `cuda/zone_nbr_store.cuh` (NEW, device-FREE: ноль __global__/cudaMalloc/<cuda_runtime.h>) — re-export примитива как `cuda::nbr_store::advance/stale` + замороженный §3.5 Tier-0/1/2 контракт doc-comment + явные DEFER-записи «потребитель приходит с каркасом»; включается из g++ CPU-теста ⇒ МЕХАНИЧЕСКИ форсит device-freeness (device-код → compile-break с потребителем). **MUST-FIX приёмки-дизайна (structurally-dead):** re-export был инертен-но-нетестируем ⇒ T-SCAFFOLD зовёт `cuda::nbr_store::advance/stale` ЧЕРЕЗ alias (удаление using ломает тест). Гейты: `Test_Rebuild_Epoch` 7 (T1 advance-tick-iff-rebuild, T2 монотонность, T3 L-SUP-мост «строго растёт⟺rebuild», T4 stale-strict-less, T5 lazy-reuse-conservativeness интеграция, T6 детерминизм, T-SCAFFOLD alias) + чистые ребилды CPU 45/45 + CUDA 17/17 + **CUDA conveyor 8/8 ПОБИТОВО** (поле едет в GpuHeader — LJ 1-vs-N streams, verlet ×5, Replica36 25900 шагов 1-vs-4, mixed 1-vs-z) + **MPI np2/np4 2/2** (кросс-процессная wire-parity). Байт-нетронуты: conveyor_gpu.cuh/mpi_ring_edge.hpp/skin_budget.hpp/fsm.hpp/все кольца/силовые пути; поле default-0-never-read. **DEFER к PR-4 (правило consumer-arrives):** пер-ранговый local_epoch[] device-state, Tier-0/1/2 CSR-буферы+G-B фикстура, epoch WRITE/READ в боевом кольце + G-EPOCH (verlet_hybrid=ON, локально). Zero cloud-compile burden (device-код нулевой). **Дальше:** PR-2 (донации в живом EamRing) / PR-3a-b (GPU-донации, гейт R_W≥1.15) / PR-4 (Λ-скин-порт + Tier-2 + активация эпохи — теперь на готовых skin_budget.hpp + rebuild_epoch.hpp).

**PR-2 W-КОНТРАКТА — ЖИВОЕ ДОНАЦИОННОЕ КОЛЬЦО EamRing ВЫПОЛНЕНО (2026-07-03, ветка `opus`; состяз. дизайн `wf_3aad7a4b` [4 читателя→2 дизайнера→судья→4 верификатора, все ACCEPT-WITH-FIXES — 4 MUST-FIX вшиты] + приёмка `wf_7b593d85` [bitwise-orch/fork-fnoop/gate-mechanism ACCEPT, teeth ACCEPT-WITH-FIXES — MUST-FIX применён]; дизайн-док `_meta/PR2_EAMRING_DONATIONS_DESIGN_2026-07-03.md`).** РЕАЛЬНЫЙ w-выигрыш в живом кольце: проверенный serial-оракул PR-1 вшит в тредовое `EamRing` — ρ собирается донационными int64-батчами в scan-цикле (self на drift-фронте, cross на ко-резидентности — arrival-вариант §9) в пер-зонное `EamDonationState<DA>` (ключ zone_id/label, pass-scoped, reset на RECV, mid-pass HALT отбрасывает проход); `finalize_owned` КОМПОНУЕТ силу из персистентной ρ вместо ×3-пересчёта. **L1-ДОКАЗАТЕЛЬСТВО (несущее):** 14 существующих ring-тестов ПОБИТОВО зелены ⇒ донационное кольцо ≡ `zone_eam_pass` транзитивно (донационное==serial-VV ∧ zone_eam_pass байт-заморожен ∧ old_ring==serial-VV ⇒ donated==old кросс-платформенно). **Ключевые решения:** (1) `run_pass`→`run_pass_impl<DA>` (fb-диспатч 44/40 от density_fracbits); (2) ГЕЙТ §12.5 = ВРЕМЕННЫЙ dual-path через POLICY-SWAP (test-only `CpuEamRecomputeWinForce` игнорит ρ и пересчитывает — CPU-аналог GPU-политики; изолирует ровно density-сборку: donate_position/ledger/SPHERE-gate в РИНГЕ policy-agnostic ⇒ гоняются в обеих политиках, разница только compose-из-ρ vs recompute); старый путь ИСЧЕЗАЕТ (кольцо больше не recompute-ит density); (3) concept-хуки on_zone_arrival/on_edge/compose — в РЕФАЙНМЕНТ `DonatingWindowForcePolicy` (НЕ база `WindowForcePolicy` — иначе sw/tersoff/meam-политики ломаются; audit-D2 «хуки с потребителем»); base байт-нетронут ⇒ siblings целы; (4) материальный SPHERE — d→w гейтится завершённостью density-леджера (`ledger[label]==want_closure_mask`) СТРОГО перед SPHERE/END, артифициальность снята. **GPU (K6):** `GpuEamWindowForce` +3 хука (on_*/compose инертны — compose ИГНОРИТ ρ_w и recompute-ит на device) ⇒ `EamGpuRing` output ПОБИТОВО == сегодня (device-донация — PR-3b, гейт R_W≥1.15). **4 MUST-FIX приёмки-дизайна вшиты:** (M1) `block_view.key` — `int*` vs `const long*` ⇒ `Slot::gkey` материализуется на arrival (nullptr был бы null-deref: `on_pair(key[t])` вычисляется даже под NullPairHook); (M2) doc-таблица леджера zone-0 (код close_cross_batch верен: шов n-1→CrossHi, интерьер 0→CrossHi, zone0=7); (M3) G8 drift-band ИЗМЕРЕН на живом кольце (снятие стража ⇒ StaleZone не бьёт + траектория расходится с FP64 на 2.96 Å за 2 шага >> 1e-9 — класс-3, non-vacuous); (M4) EAM-баннер (eam_ring.hpp) документирует необратимое расхождение с siblings + помечает их «swap-only» доказательства superseded-historical (siblings байт-целы). **Fork:** EAM scan-loop необратимо расходится с sw/tersoff/meam (байт-целы; унификация — ReaxFF, форк №5). **MUST-FIX ПРИЁМКИ (6-й рецидив structurally-dead):** снятие ledger-before-END гейта (ядро материального SPHERE) не валило НИ ОДИН из 20 тестов — несущий страж был без автоматического зуба ⇒ добавлен test-only knob `test_drop_first_self_ledger_` (default false, инертен) + G10LedgerBeforeEndGate (дроп первого self-бита ⇒ Halt::StaleZone с «donation ledger»-диагностикой); мутационно верифицирован (без гейта G10 КРАСНЫЙ). **Гейты:** CPU 45/45 (Test_EAM_Ring 21 = 14 L1 + G1 dual-path/G4 FP64-оракул/G5 rotation-≥2-оборота/G7 rho_cap/G8 drift-band/G9 one-sided-poison/G10 ledger-gate; Test_EAM_Donation 15/15 неизменны) + CUDA: ctest -L cuda 17/17 целиком + санитайзер (memcheck+racecheck) чист СКОУПОМ на 4 затронутых GPU-TU (eam_ring/meam/meam_cells/meam_ring — все TU, включающие правленные хедеры; двухуровневый гейт, см. ниже). Байт-нетронуты: zone_eam_pass/eam_window_force (оракул), sw/tersoff/meam-кольца, fsm.hpp, many_body.hpp (база), conveyor.hpp. FSM-аддендум §12 п.6 (append, fsm.hpp цел). **Попутно — gpu_gate.sh стал двухуровневым (2026-07-07, отдельный коммит):** замеры показали — санитайзерный цикл, не тесты, съедает время (raw ctest ~6 мин; memcheck ×5, racecheck ×2–17 — оверхед ∝ числу ЗАПУСКОВ ядер, поэтому 3000-шаговые траектории взрываются: memcheck на conveyor `Mixed+OffEq` один — 575 с; исторические «часы» = до-фикса racecheck OffEquilibrium 2h+ и прогоны под GPU-конкуренцией ×2–3). Скрипт: (а) необязательные глобы бинарей сужают ТОЛЬКО санитайзер (ctest -L cuda всегда целиком; SCOPED-баннер в лог; пустой матч = ошибка, не вакуумный зелёный) — полный свип остаётся пре-мердж обязательным; (б) санитайзерные задания (бинарь×тул) параллелятся (GPU_GATE_JOBS=4; вердикт наличие-ошибок от контеншна не зависит — это не перф-харнесс; пер-джобные логи). Полный свип ~60 мин → ~20; PR-итерация — минуты. **Дальше:** PR-3a/b (GPU-донации, device-резидентная ρ, гейт R_W≥1.15 с NULL-откатом) / PR-4 (Λ-скин-порт + Tier-2 + активация эпохи).

**PR-3a W-КОНТРАКТА — DEVICE-ДОНАЦИОННЫЙ СУБСТРАТ GPU-EAM-КОЛЬЦА ВЫПОЛНЕН (2026-07-09, ветка `opus`; состяз. дизайн `wf_e3923bb3` [4 читателя→2 дизайнера→судья→4 верификатора; поправки M1-M9/S1-S13/OQ1-OQ7 вшиты в замороженный док] + приёмка `wf_1c8cf3af` [bitwise ACCEPT-WITH-FIXES / teeth ACCEPT / fnoop ACCEPT / spec ACCEPT-WITH-FIXES — все 3 MUST-FIX вшиты]; дизайн-док `_meta/PR3AB_GPU_DONATION_DESIGN_2026-07-07.md` + append-only realization-notes).** Инфраструктурный шаг W-трека (НЕ перф): донационная ρ садится на device. `GpuEamDonationNodeStore` (пер-узловой, LABEL-ключёванные `ZoneLane` — device-слэбы позиций POST-drift 24 Б/атом + персистентные int64 ρ-лейны + stamp/token staleness-fence [посадочное место rebuild_epoch PR-4]; все grow-пути sync-before-free M9); SELF-батч = замороженные `eam_density[_cells]_kernel` ДОСЛОВНО по слэбу (его overwrite-запись = per-pass reset по построению); CROSS = ЕДИНСТВЕННАЯ новая пара ядер `cuda/zone_eam_donation.cuh` (стенсиль VERBATIM из cells-ядра + РОВНО две дельты: same-side skip `(bb<na)==(aa<na)` и двухсторонний `+=`-эпилог; побитовость к CPU-экзекьюторам PR-1 по лемме W-1+B1+L-SUP). ВСЁ за ctor-knob `donate_device=false` — дефолт байт-идентичен HEAD (A0/A8 по построению); при ON ядра гоняются НАБЛЮДАТЕЛЬНО (compose recompute-ит; композиция из лейнов = PR-3b, гейт R_W≥1.15). Концепт v2 `DonatingWindowForcePolicy` (`NodeState`/`make_node_state`/`begin_pass`, ns-first хуки, `compose(ns,…,WindowBlocks,…)`); Tier-0 реализован ВТОРОЙ санкционированной формой аудита (population-refresh whole-box CSR per-батч — drift-binning снят по построению, G-B = гейт A5), Tier-1 slab-residency сел, view-concat-потребление DEFER→M5b (нота в `zone_nbr_store.cuh`). **Гейты A0-A7** (`Test_CUDA_EAM_Donation`, 8): A0 knob-ON≡OFF побитово (траектория+PE, 5 конфигов) + счётчики-невакуумность + негативная полоса A6 (`donation_overflow()==0` на легальном прогоне — вшита приёмкой); A1 device-лейны ≡ CPU-донированной ρ СЫРЫМ int64 на 7 ногах (free n∈{1,2,4}, pbc n=5 z∈{1,3}, **cull=true И cull=false**) + киллы stencil/one_sided в-гейте; A2 шов-арифметика; A3 fb=40; A4 вакуум пустых зон; A5 дрейф-донор через границу зоны (G-B; килл drop_outside_nominal_slab); A6 overflow-plumbing (respec: single-quantum setfl НЕ ЗАГРУЖАЕТСЯ — страж `density_fracbits()` делает бит-1 defense-in-depth; порча rhoaspl ×1e7 после легальной загрузки); A7 stale-stamp THROW. **MUST-FIX приёмки — 7-й рецидив structurally-dead:** ВСЕ фикстуры были cull=true ⇒ plain `eam_donate_cross_kernel` шёл БЕЗ свидетеля (ревьюер выпотрошил тело — 8/8 зелёные!); фикс = cull=false ноги A1, килл re-verified (потрошение ⇒ A1 красный). Другие MUST-FIX (spec): Axis B в Bench был подписан N=4096 при факте N=14976; A8-сужение против заморозки записано + HEAD-нога primary-grid исполнена worktree-бинарём с PR-харнессом (apz∈{1997,7904} back-to-back; большие точки исключены измеренно). **A8 wall-гейт: PASS, max |Δ|=1.9%≤3%** (PR- vs HEAD-бинарь, дефолтные оси + Axis B + primary-2pt, reps=8, idle). **Bench §PR-3a:** Axis G бейзлайн замороженной R_W-сетки (2.997e5/8.129e5/9.484e5/1.188e6 @ apz 1997/7904/21299/42120); **метод-находка (perf):** стенка больших точек = t0-инициализация `EamRing::run()` (serial CPU `zone_eam_pass`, O(N²)-класс, 65% eam_window_force+31% __round) — оценщик её вычитает (A валидны), но wall ∝N² (apz=42120 ~2.5 ч/ногу; R_W-бенч 3b — закладывать часы; кандидат-рычаг [ENG] t0-через-GPU отложен measure-first); **вариативность host-bound точек ±25% МЕЖДУ бутами** (apz=21299: 1.265e6 до ребута vs 9.484e5 после) при внутрибутовой ≤1.3% ⇒ 3b обязан отчитать пер-реповый разброс. **J10 initcheck (записанный прогон):** один НОВЫЙ класс — RMW `+=` читал неинициализированный discard-sink пуазона (тест-only; зачинен cudaMemset в `discard_lane`), донационные ядра после фикса 0 хитов; остаток 10464 — унаследованный uninitialized-D2H в recompute-`compute()` (задокументирован SW T5, не расширен). Скоуп-гейт: ctest -L cuda 18/18 + memcheck/racecheck 5 бинарников чисто; CPU 45/45. Байт-нетронуты: sw/tersoff/meam-кольца+политики, база `WindowForcePolicy`, `zone_eam.cuh`/`zone_eam_cells.cuh`/`zone_cells.cuh`, fsm/conveyor/skin_budget/rebuild_epoch. **Дальше:** PR-3b (донированный compose: D2D-скаттер лейнов + embedding_nocap + owned_cap, силовые ядра байт-целы; два коммита c1/c2; R_W≥1.15 по замороженному протоколу, NULL ⇒ revert c2; `eam_ring.hpp`+`eam_donation.hpp` байт-заморожены) / PR-4 (Λ-скин-порт + Tier-2 + активация эпохи).
